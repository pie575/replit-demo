---
title: "在 Python 和 Flask 中使用 Google 身份验证"
description: "了解如何在 Replit 上的 Flask 应用中实现 Google OAuth 身份验证，包括用户登录和 Google Sheets API 集成。"
---

让用户使用 Google 账号登录你的网站有以下好处：

1. 你无需自行实现身份验证方案。
2. 你可以轻松获取用户的姓名和联系方式。
3. 你可以使用同一套凭据访问用户的 Google 资源，例如 Sheets 和 Drive。

本指南将带你了解如何在 Replit 上使用 Python 和 Flask 实现这一功能。

我们先介绍如何设置基本的 OAuth 身份验证，然后讲解如何使用获得的凭据访问用户的 Google 资源。

<div id="introduction-to-oauth">
  ## OAuth 简介
</div>

Google 身份验证基于 OAuth 标准。OAuth 的工作流程如下：

1. 在网站的某个位置，将用户引导至登录页面。
2. 用户进入登录页面后，登录表单并不由你的网站实现，而是重定向到 Google 的登录服务来完成用户登录。
3. Google 的登录服务成功登录用户后，会重定向回你预先指定的网站 URL，例如 `https://YOUR_DOMAIN/oauth2callback`，同时附带与该用户和本次登录会话相关的信息。
4. 你可以使用这些登录信息进一步获取访问令牌。该令牌相当于一张通行证，可用于访问用户的资源，例如个人资料信息、电子表格、文档等。

<div id="oauth-show-me-the-code">
  ## OAuth：直接上代码
</div>

如果你和我一样，最想先看到的就是能跑起来的代码。下面就是你需要的代码。不过，你还需要在 Google Cloud Console 中做一些配置，才能让一切正常运行，这部分将在下一节介绍。使用 [Flask 模板](https://replit.com/@replit/Flask?v=1)创建一个新的 Replit App，并将以下内容放入 `main.py`。代码中的注释说明了各个部分的作用：

```python theme={null}
from flask import Flask, redirect, session, url_for, request
import google_auth_oauthlib.flow
import json
import os
import requests

app = Flask('app')
# `FLASK_SECRET_KEY` 供会话使用。你应当生成一个随机字符串，
# 并将其保存为密钥。
app.secret_key = os.environ.get('FLASK_SECRET_KEY') or os.urandom(24)

# `GOOGLE_APIS_OAUTH_SECRET` 的内容来自需从 Google Cloud 凭据面板下载的
# JSON 文件。详见下一节。
oauth_config = json.loads(os.environ['GOOGLE_OAUTH_SECRETS'])

# 这里为 OAuth 流程设置配置
oauth_flow = google_auth_oauthlib.flow.Flow.from_client_config(
    oauth_config,
    # scopes 定义了认证完成后你希望代表用户访问哪些 API
    scopes=[
        "https://www.googleapis.com/auth/userinfo.email",
        "openid",
        "https://www.googleapis.com/auth/userinfo.profile",
    ]
)

# 这是登录页面的入口点。它会重定向到位于 `authorization_url` 的 Google 登录服务。
# `redirect_uri` 实际上是 Google 登录服务用来重定向回本应用的 URI。
@app.route('/signin')
def signin():
    # 这里把 URL 从 http 改写为 https，因为 Replit App 内部使用 http，
    # 而外部是通过 https 访问的，redirect_uri 必须与之一致
    oauth_flow.redirect_uri = url_for('oauth2callback', _external=True).replace('http://', 'https://')
    authorization_url, state = oauth_flow.authorization_url()
    session['state'] = state
    return redirect(authorization_url)

# 这是 Google 登录服务重定向回来的端点。必须把它添加到 Google Cloud 的 API 凭据面板中的
# “Authorized redirect URIs”中。它会调用 Google 的端点请求访问令牌，
# 并将其保存在用户会话中。此后即可使用该访问令牌代表用户访问 API。
@app.route('/oauth2callback')
def oauth2callback():
    if not session['state'] == request.args['state']:
        return 'Invalid state parameter', 400
    oauth_flow.fetch_token(authorization_response=request.url.replace('http:', 'https:'))
    session['access_token'] = oauth_flow.credentials.token
    return redirect("/")

# 这是应用的主页。如果用户尚未登录，就引导其登录；
# 如果已登录，则显示用户信息。
@app.route('/')
def welcome():
    if "access_token" in session:
        user_info = get_user_info(session["access_token"])
        if user_info:
            return f"""
                Hello {user_info["given_name"]}!<br>
                Your email address is {user_info["email"]}<br>
                <a href="/logout">Log out</a>
            """
    return """
        <h1>Hello!</h1>
        <a href="/signin">Sign In via Google</a><br>
    """

# 使用有效的访问令牌调用 userinfo API 获取用户信息。
# 这是代表用户使用访问令牌访问 API 的第一个示例。
def get_user_info(access_token):
    response = requests.get("https://www.googleapis.com/oauth2/v3/userinfo", headers={
       "Authorization": f"Bearer {access_token}"
   })
    if response.status_code == 200:
        user_info = response.json()
        return user_info
    else:
        print(f"Failed to fetch user info: {response.status_code} {response.text}")
        return None

@app.route('/logout')
def logout():
    session.clear()
    return redirect('/')

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)

```

<div id="set-up-your-oauth-app-client">
  ## 设置你的 OAuth App / 客户端
</div>

要让上面的代码正常运行，你需要在 Google Cloud 中完成以下操作。

1. 创建一个 Google Cloud 项目 (如果还没有) 。
2. 配置 OAuth 同意屏幕。
3. 为你的应用创建一个 OAuth Client ID。

<div id="create-a-google-cloud-project">
  ### 创建 Google Cloud 项目
</div>

如果你已有想用于本练习的 Google Cloud 项目，可以跳过此步骤。

1. 前往 [Google Cloud Console](https://console.cloud.google.com/)
2. 点击 Google Cloud 徽标旁的项目选择器下拉框：

<Frame>
  <img src="/assets/mirror/google-project-dropdown-d64bd797ae82.png" alt="Google 项目下拉框" width="734" height="94" data-path="images/google-auth-in-flask/google-project-dropdown.png" />
</Frame>

3. 选择一个现有项目，或点击“New Project”新建一个项目。

<Frame>
  <img src="/assets/mirror/select-project-22c9b948c348.png" alt="选择项目" width="1092" height="868" data-path="images/google-auth-in-flask/select-project.png" />
</Frame>

4. 如果要新建项目，请输入项目名称，然后点击“Create”。

<Frame>
  <img src="/assets/mirror/new-project-8a60880bda21.png" alt="新建项目" width="1110" height="868" data-path="images/google-auth-in-flask/new-project.png" />
</Frame>

如果弹出窗口中显示了你的新项目，点击“Select project”将其设为*活动项目*。

<div id="configure-the-oauth-consent-screen">
  ### 配置 OAuth 同意屏幕
</div>

现在你已经有了一个项目，可以为它配置 OAuth 同意屏幕：

1. 前往 [OAuth 同意屏幕](https://console.cloud.google.com/apis/credentials/consent)

<Frame>
  <img src="/assets/mirror/oauth-consent-screen-8c5d7264f4ba.png" alt="OAuth 同意屏幕" width="1196" height="675" data-path="images/google-auth-in-flask/oauth-consent-screen.png" />
</Frame>

2. 确认项目下拉菜单中选中的是你要用的那个项目。
3. 选择 &quot;External&quot;，让任何用户都能使用 Google 账户登录你的应用；选择 &quot;Internal&quot; 则只允许你所在组织的成员登录。
4. 点击 &quot;Create&quot;。
5. 输入应用名称，以及该应用支持人员的邮箱 (应该就是你吧？) 

<Frame>
  <img src="/assets/mirror/oauth-consent-step-2-f00ee976e9fd.png" alt="OAuth 同意屏幕" width="1196" height="675" data-path="images/google-auth-in-flask/oauth-consent-step-2.png" />
</Frame>

6. 在 &quot;Developer contact information&quot; 下填写一个邮箱地址。

<Frame>
  <img src="/assets/mirror/oauth-consent-step-2-b-a2f1a259f431.png" alt="开发者联系信息" width="533" height="207" data-path="images/google-auth-in-flask/oauth-consent-step-2-b.png" />
</Frame>

7. 点击 &quot;Save and continue&quot;。
8. 在 Scopes 页面中，你可以添加希望应用访问的 API。用于获取用户基本信息的 API 你已经有访问权限了。

<Frame>
  <img src="/assets/mirror/oauth-scopes-f99364e44221.png" alt="OAuth 权限范围" width="1093" height="647" data-path="images/google-auth-in-flask/oauth-scopes.png" />
</Frame>

这里暂时保持默认即可，点击 &quot;Save and continue&quot;。
9. 在 Test Users 中，添加你希望在测试阶段试用该应用的用户邮箱。

<Frame>
  <img src="/assets/mirror/test-users-048f029f4d9f.png" alt="测试用户" width="1050" height="641" data-path="images/google-auth-in-flask/test-users.png" />
</Frame>

点击 &quot;Add users&quot;
10. 添加一个或多个 Google 邮箱地址，然后点击 &quot;Add&quot;。

<Frame>
  <img src="/assets/mirror/test-users-add-43e7efabac17.png" alt="测试用户" width="1050" height="641" data-path="images/google-auth-in-flask/test-users-add.png" />
</Frame>

然后点击 &quot;Save and continue&quot;。
11. 查看摘要页面。你随时可以返回修改前面的任何一步。

<Frame>
  <img src="/assets/mirror/summary-54322ee205dd.png" alt="测试用户" width="735" height="328" data-path="images/google-auth-in-flask/summary.png" />
</Frame>

<div id="create-an-oauth-client-id-for-your-app">
  ### 为你的应用创建 OAuth 客户端 ID
</div>

这是最后一步。要让 OAuth 正常工作，你需要为该应用创建一个 OAuth 客户端 ID。

1. 前往 [Credentials](https://console.cloud.google.com/apis/credentials)。

<Frame>
  <img src="/assets/mirror/credentials-37967178ae47.png" alt="Test Users" width="1050" height="605" data-path="images/google-auth-in-flask/credentials.png" />
</Frame>

2. 点击 &quot;Create credentials&quot;

<Frame>
  <img src="/assets/mirror/create-credentials-411c8b243090.png" alt="Test Users" width="1050" height="605" data-path="images/google-auth-in-flask/create-credentials.png" />
</Frame>

选择 &quot;OAuth client ID&quot;。
3.  在 Application type 中选择 &quot;Web application&quot;，并为该客户端 ID 输入一个名称。

<Frame>
  <img src="/assets/mirror/create-credentials-web-53cd170ef6ff.png" alt="Test Users" width="1050" height="605" data-path="images/google-auth-in-flask/create-credentials-web.png" />
</Frame>

4. 现在回到你的 Flask Replit App，打开 Shell 并输入：`echo https://$REPLIT_DEV_DOMAIN/oauth2callback`。输出结果类似：`https://81309e9b-c4df-48e0-a2c2-0a8d3c0e3162-00-35ppsa0tcuv6v.infra-staging.replit.dev/oauth2callback`。复制这段文本，填入表单底部的 &quot;Authorized redirect URIs&quot; 中

<Frame>
  <img src="/assets/mirror/authorized-redirect-uris-6a5811acd49a.png" alt="Test Users" width="800" height="271" data-path="images/google-auth-in-flask/authorized-redirect-uris.png" />
</Frame>

以后发布应用时，还需要回到这里再添加一条 `https://YOUR_APP_DOMAIN/oauth2callback`

5. 点击 &quot;Create&quot;
6. 点击 &quot;Download JSON&quot;：

<Frame>
  <img src="/assets/mirror/secrets_json-d507ecb4ea34.png" alt="Test Users" width="573" height="607" data-path="images/google-auth-in-flask/secrets_json.png" />
</Frame>

7. 再次回到你的 Replit App，打开 Secrets 面板。创建一个名为 `GOOGLE_OAUTH_SECRETS` 的密钥，把下载文件的内容粘贴
   为该密钥的值。

<Frame>
  <img src="/assets/mirror/add-secret-92f7d85d5156.png" alt="Test Users" width="1060" height="677" data-path="images/google-auth-in-flask/add-secret.png" />
</Frame>

呼！步骤确实有点繁琐。能坚持到这里，恭喜你！现在可以运行这个 Flask 应用，并用测试用户的 Google 账号登录了。如果想让所有 Google 用户都能使用你的应用，还需回到[同意页面](https://console.cloud.google.com/apis/credentials/consent)并点击 &quot;Publish App&quot;。如果应用还需要 Sheets、Drive 等其他 Google API，可能要经过一轮审核验证。

接下来，我们会介绍如何与 Sheets 这类 Google API 集成。想更进一步的话，就继续跟着操作吧。

<div id="google-sheets-api-setup">
  ## Google Sheets API 设置
</div>

要添加 Google Sheets 这类 Google API 集成，首先需要为应用启用相应的 API。你可以[浏览可用的 API](https://console.cloud.google.com/apis/library)。这里我们以 Google Sheets 为例。

1. 前往 [Google Sheets API 页面](https://console.cloud.google.com/apis/library/sheets.googleapis.com)。
2. 点击「Enable」。

完成！这一部分需要做的 Google Cloud 设置就这些。

<div id="google-sheets-integration-show-me-the-code">
  ## Google Sheets 集成：直接看代码
</div>

首先，在原始代码的 OAuth 流程部分，我们保持其余内容不变，只在 scopes 列表中添加 `"https://www.googleapis.com/auth/spreadsheets.readonly"`：

```python theme={null}
# 这里为 OAuth 流程配置相关参数
oauth_flow = google_auth_oauthlib.flow.Flow.from_client_config(
    oauth_config,
    # scopes 定义认证完成后，你希望代表用户访问哪些 API
    scopes=[
        "https://www.googleapis.com/auth/userinfo.email",
        "openid",
        "https://www.googleapis.com/auth/userinfo.profile",
        "https://www.googleapis.com/auth/spreadsheets.readonly"
    ]
)
```

现在，使用 `googleapiclient.discovery` 库访问 Google API 的方式是：先用访问令牌创建一个 `Credentials` 对象，再用 `build` 函数创建一个可调用的 API 对象。以 Sheets API 为例，代码如下：

```python theme={null}
credentials = google.oauth2.credentials.Credentials(token=session['access_token'])
service = build("sheets", "v4", credentials=credentials)
sheets_api = service.spreadsheets()
```

至于具体如何使用 Sheets API，我写了几个辅助函数：

```python theme={null}
# 获取 Google 电子表格中的所有工作表
def get_sheets(sheets_api, spreadsheet_id) -> list[str]:
    result = sheets_api.get(spreadsheetId=spreadsheet_id).execute()
    return [sheet["properties"]["title"] for sheet in result["sheets"]]

# 获取 Google 电子表格中指定工作表的数据
def get_sheet_data(sheets_api, spreadsheet_id, sheet_title) -> list[list[str]]:
    result = (
        sheets_api.values()
        .get(spreadsheetId=spreadsheet_id, range=sheet_title)
        .execute()
    )
    return result["values"]
```

有了上面这些内容，我们就可以创建一个用于导入 Google 电子表格的 POST 处理端点，如下所示：

```python theme={null}
@app.route("/import_spreadsheet", methods = ['POST'])
def import_spreadsheet():
    if 'access_token' not in session:
        return redirect('/signin')
    spreadsheet_id = request.form["spreadsheet_id"]
    credentials = google.oauth2.credentials.Credentials(token=session['access_token'])
    service = build("sheets", "v4", credentials=credentials)
    sheets_api = service.spreadsheets()
    try:
        sheets = get_sheets(sheets_api, spreadsheet_id)
        data_by_sheets = {}
        for sheet in sheets:
            data = get_sheet_data(sheets_api, spreadsheet_id, sheet)
            data_by_sheets[sheet] = data
    except googleapiclient.errors.HttpError as e:
        return f"upload failure"
    dirpath = os.path.join("static", "uploads", spreadsheet_id)
    filepath = os.path.join(dirpath, "data.json")
    os.makedirs(dirpath, exist_ok=True)
    with open(filepath, "w") as file:
        json.dump(data_by_sheets, file)
    return "upload success!"
```

以下是完整的可运行代码：

```python theme={null}
from flask import Flask, redirect, session, url_for, request
import google_auth_oauthlib.flow
import json
import os
import requests
from googleapiclient.discovery import build
import googleapiclient.errors
import google.oauth2.credentials

app = Flask('app')
# 会话会使用 `FLASK_SECRET_KEY`。你应当生成一个随机字符串，
# 并将其保存为 secret。
app.secret_key = os.environ.get('FLASK_SECRET_KEY') or os.urandom(24)

# `GOOGLE_APIS_OAUTH_SECRET` 保存的是从 Google Cloud 凭据面板下载的
# JSON 文件内容。详见下一节。
oauth_config = json.loads(os.environ['GOOGLE_OAUTH_SECRETS'])

# 这里为 OAuth 流程设置配置
oauth_flow = google_auth_oauthlib.flow.Flow.from_client_config(
    oauth_config,
    # scopes 定义认证通过后，你希望代表用户访问哪些 API
    scopes=[
        "https://www.googleapis.com/auth/userinfo.email",
        "openid",
        "https://www.googleapis.com/auth/userinfo.profile",
        "https://www.googleapis.com/auth/spreadsheets.readonly"
    ]
)

# 这是登录页面的入口点。它会重定向到位于 `authorization_url` 的
# Google 登录服务。而 `redirect_uri` 则是 Google 登录服务用来
# 重定向回本应用的 URI。
@app.route('/signin')
def signin():
    # 这里把 URL 从 http 改写为 https：Replit App 内部使用 http，
    # 但外部通过 https 访问，redirect_uri 必须与外部地址一致
    oauth_flow.redirect_uri = url_for('oauth2callback', _external=True).replace('http://', 'https://')
    authorization_url, state = oauth_flow.authorization_url()
    session['state'] = state
    return redirect(authorization_url)

# 这是 Google 登录服务重定向回来的端点。必须把它添加到 Google Cloud 的 API 凭据面板中的
# “已授权的重定向 URI”里。它会调用 Google 的端点来请求访问令牌，
# 并将令牌存入用户会话。此后即可使用该访问令牌代表用户访问
# API。
@app.route('/oauth2callback')
def oauth2callback():
    if not session['state'] == request.args['state']:
        return 'Invalid state parameter', 400
    oauth_flow.fetch_token(authorization_response=request.url.replace('http:', 'https:'))
    session['access_token'] = oauth_flow.credentials.token
    return redirect("/")

# 使用有效的访问令牌调用 userinfo API，获取用户信息。
# 这是使用访问令牌代表用户访问 API 的第一个示例。
def get_user_info(access_token):
    response = requests.get("https://www.googleapis.com/oauth2/v3/userinfo", headers={
       "Authorization": f"Bearer {access_token}"
   })
    if response.status_code == 200:
        user_info = response.json()
        return user_info
    else:
        print(f"Failed to fetch user info: {response.status_code} {response.text}")
        return None

@app.route('/logout')
def logout():
    session.clear()
    return redirect('/')

# 获取某个 Google 电子表格中的所有工作表
def get_sheets(sheets_api, spreadsheet_id) -> list[str]:
    result = sheets_api.get(spreadsheetId=spreadsheet_id).execute()
    return [sheet["properties"]["title"] for sheet in result["sheets"]]

# 获取 Google 电子表格中指定工作表的数据
def get_sheet_data(sheets_api, spreadsheet_id, sheet_title) -> list[list[str]]:
    result = (
        sheets_api.values()
        .get(spreadsheetId=spreadsheet_id, range=sheet_title)
        .execute()
    )
    return result["values"]

# 渲染一个用于导入电子表格的表单
@app.route("/import_spreadsheet_form")
def import_spreadsheet_form():
    return """
    <h3>Import Spreadsheet</h3>
    <form action="/import_spreadsheet" method="POST">
        <label>Spreadsheet ID</label>
        <input type="text" name="spreadsheet_id">

        <button type="submit">Import</button>
    </form>
    """

@app.route("/import_spreadsheet", methods = ['POST'])
def import_spreadsheet():
    if 'access_token' not in session:
        return redirect('/signin')
    spreadsheet_id = request.form["spreadsheet_id"]
    credentials = google.oauth2.credentials.Credentials(token=session['access_token'])
    service = build("sheets", "v4", credentials=credentials)
    sheets_api = service.spreadsheets()
    try:
        sheets = get_sheets(sheets_api, spreadsheet_id)
        data_by_sheets = {}
        for sheet in sheets:
            data = get_sheet_data(sheets_api, spreadsheet_id, sheet)
            data_by_sheets[sheet] = data
    except googleapiclient.errors.HttpError as e:
        return f"upload failure"
    dirpath = os.path.join("static", "uploads", spreadsheet_id)
    filepath = os.path.join(dirpath, "data.json")
    os.makedirs(dirpath, exist_ok=True)
    with open(filepath, "w") as file:
        json.dump(data_by_sheets, file)
    return "upload success! Really!"

@app.route('/')
def welcome():
    if "access_token" in session:
        user_info = get_user_info(session["access_token"])
        if user_info:
            return f"""
            Hello {user_info["given_name"]}!<br>
            Your email address is {user_info["email"]}<br>
            <a href="/signin">Sign In to Google</a><br>
            <a href="/import_spreadsheet_form">Import a Sheet</a>
            """
    return """
    <h1>Welcome to Google Sheet Importer</h1>
    <a href="/signin">Sign In to Google</a><br>
    <a href="/import_spreadsheet_form">Import a Sheet</a>
    """

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
```

请记住，如果你要发布这个应用，务必：

1. 将生产环境的 `/oauth2callback` URI 添加到“已获授权的重定向 URI”中。
2. 前往[同意页面](https://console.cloud.google.com/apis/credentials/consent)，点击“Publish App”。

希望这次体验还不错，也祝你在接下来的探索中一切顺利。