---
title: "Prototyper une idée de produit"
description: "Transformez une idée de produit en un prototype ciblé, collaborez avec d'autres personnes et appuyez-vous sur les retours pour décider de la suite à donner."
---

export const PACE_PROMPT = `Build me a personal running web app called "Pace".

I want a clean, modern web app where I can track my runs and see my progress at a glance — a friendlier, more visual version of a running journal like Strava or Nike Run Club, but simpler and prettier. It only needs to support one user (me) for now, and my profile name should be Alex Morgan. Pre-fill it with about a month of pretend running history so it looks alive from the moment I open it — runs through San Francisco, a mix of easy runs, tempo runs, long runs, and recovery runs, with at least 4–5 runs in the past week so the dashboard looks active.

The app should run in any modern browser, feel fast, and be fully responsive — great on desktop, tablet, and phone.

Look and feel
The vibe is sporty, premium, "athletic brand" — black, white, and a punchy lime green as the accent color. Use the Inter font everywhere. Rounded corners on everything (cards, buttons, pills). It should feel calm and uncluttered, not busy. Light mode by default. Smooth, subtle animations when pages transition, when cards appear, and when I click buttons — it should feel responsive and alive, not stiff.

Overall navigation
A top nav bar with the Pace logo on the left and 5 links across the middle: Home, Runs, Plan, Stats, Profile. The active link has a lime underline; inactive links are gray. On the right: a lime "Log a Run" button (always visible) and a round profile photo (clicking it opens Profile).

On smaller screens, the middle nav links collapse into a hamburger menu, but the lime "Log a Run" button stays visible.

The Home page (the main page) — a scrollable feed, top to bottom:

Greeting. A friendly greeting that changes with the time of day ("Good morning, Alex" / "Good afternoon, Alex" / "Good evening, Alex"), with a subtitle "Ready for your next run?".

Hero banner card. A wide photo of someone running on pavement, with a dark gradient on the left so the text is readable. Big white headline: "You don't find willpower, you create it." Underneath: "Keep showing up." Bottom-left: a lime pill button "Start a Run" with a little black play-circle. Clicking it opens the Log a Run dialog.

"This Week" stats. A row of four cards (2×2 grid on smaller widths): Distance (km), Time (h/min), Elevation (m), Runs (count). Each card: small icon, big number, "+12% vs last week" caption, thin squiggly line graph at the bottom (lime, blue, purple, lime). Numbers reflect my actual runs from the last 7 days.

Weekly Progress card. Title + small "View goals" link. Then "X of Y runs completed" and a big bold percentage on the right. Gradient lime progress bar. Below: seven day pills (M T W T F S S) with date numbers. Today is a black filled circle; days I've run show a lime circle with a white checkmark; others are empty circles.

Your Last Run card. A real interactive map (Leaflet, Mapbox, or similar) showing the route of my most recent run as a lime line, date in the corner, lime "View Run" button, and four stats (Distance, Time, Pace, Calories). Click → full run detail page.

Recent Runs section. Title "Recent Runs" + "See all" link. Three rows, one per recent run: tiny green map-line thumbnail on the left, run name + date, distance + pace lined up on the right. Click a row → run detail. A small "Delete" icon appears on hover.

Upcoming Workout card. Title + small lime circle icon with footprints. Workout name "Tempo Run", "Tomorrow · 6:30 AM". Three stats: 8 km, 5:40/km target pace, "Moderate" intensity. A muted gray "View workout" button. (Placeholder — no real training plan yet.)

Recent Achievements card. Two badges, each with a colorful icon, the badge name, and a one-line description. First is a real PR from my history (e.g. "10K Crusher — Completed a 10K in under 60 minutes"). Second is a placeholder: "Early Bird — Completed 5 runs before 7 AM" with a lime crescent moon icon.

The Runs page
Full list of all my runs, grouped by month (sticky month headers as I scroll). Each row like the recent-runs rows. A search input in the top-right filters by name or run type (Easy, Tempo, Long, Recovery, Interval). Click a row → run detail.

The Stats page — a scrollable view with:
- Time-range pill tabs at the top (7D / 30D / 3M / 6M / 1Y / All).
- Three KPI numbers (Total Distance, Avg Pace, Total Time), each with a green or red badge for change vs the previous period.
- A smooth lime area chart of distance over time. Hover a point to see that day's value.
- Below: a small bar chart of "runs per day of week" and a list of personal records (fastest 5K, fastest 10K, longest run, etc.).

The Plan page
A weekly calendar of upcoming workouts (placeholder — mostly the visual shell). Each day is a row showing what's planned (or "Rest day"). Today's row highlighted lime.

The Profile page
Avatar, name, weekly goal setting, total all-time stats (total distance, total runs, total hours), an "Upgrade to Pro" card with a lime sparkle and "Upgrade Now" button, and a settings list (units km/mi, notifications, sign out).

The "Log a Run" dialog (modal that centers on desktop, slides up from the bottom on mobile):
- Type of run (segmented control: Easy / Tempo / Long / Recovery / Interval)
- Distance in km (number input)
- Duration (mm:ss or hh:mm:ss input)
- Date and time (date and time pickers)
- Optional name
- Big lime "Save Run" button at the bottom
After saving: smooth dismiss, dashboard updates instantly, small toast confirmation.

The Run Detail page (a dedicated page when I click a run):
- Big interactive map of the full route at the top
- Date, run type chip, back link
- Stats row (Distance, Duration, Avg Pace, Calories)
- Per-kilometer splits with pace per km
- "..." menu with Edit and Delete

How it should behave
- The greeting changes based on time of day.
- Hero "Start a Run" and the nav "Log a Run" both open the same Log a Run dialog.
- Loading skeletons instead of blank space.
- Smooth page transitions.
- Fully responsive — every screen looks great on desktop, tablet, and phone.

Things to be honest about
- No real elevation data yet — use a sensible placeholder for the elevation stat card and flag it.
- The little route thumbnails in the recent-runs list can be a generic green squiggle if the real route shape would slow the list down — the big Run Detail map should be real.
- "Upcoming Workout" and the Plan page are placeholders — no training plan engine yet.
- No live GPS tracking in this first version — runs are added by manually filling the Log a Run form.

Build the whole thing end-to-end so it's actually usable: I should be able to open the URL, see my Alex Morgan dashboard fully populated with realistic San Francisco runs, log a new run from either the hero button or the nav, and have it show up in my stats and recent runs immediately.`;

export const PromptActions = ({prompt = "", campaign = "docs-prompt-actions"}) => {
  const LZ_SRC = "https://cdnjs.cloudflare.com/ajax/libs/lz-string/1.5.0/lz-string.min.js";
  const ensureLZString = () => {
    if (typeof document === "undefined") return;
    if (window.LZString) return;
    if (document.querySelector(`script[src="${LZ_SRC}"]`)) return;
    const s = document.createElement("script");
    s.src = LZ_SRC;
    s.async = true;
    document.head.appendChild(s);
  };
  const handleCopy = async e => {
    const btn = e.currentTarget;
    if (!prompt) return;
    try {
      await navigator.clipboard.writeText(prompt);
    } catch {
      const ta = document.createElement("textarea");
      ta.value = prompt;
      document.body.appendChild(ta);
      ta.select();
      try {
        document.execCommand("copy");
      } catch {}
      document.body.removeChild(ta);
    }
    const original = btn.dataset.label || btn.innerText;
    btn.dataset.label = original;
    btn.innerText = "Copié !";
    setTimeout(() => {
      if (btn.isConnected) btn.innerText = original;
    }, 1500);
  };
  const handleBuild = () => {
    if (!prompt || typeof window === "undefined") return;
    const utm = `utm_source=replit-docs&utm_medium=docs&utm_campaign=${encodeURIComponent(campaign)}&utm_content=prompt-actions`;
    let url;
    if (window.LZString) {
      const encoded = window.LZString.compressToEncodedURIComponent(prompt);
      url = `https://replit.com/?prompt=${encoded}&referrer=replit-docs&${utm}`;
    } else {
      url = `https://replit.com/?prompt=${encodeURIComponent(prompt)}&referrer=replit-docs&${utm}`;
    }
    window.open(url, "_blank", "noopener,noreferrer");
  };
  ensureLZString();
  const baseButtonStyle = {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    padding: "8px 16px",
    borderRadius: "8px",
    fontSize: "14px",
    fontWeight: 500,
    cursor: "pointer",
    lineHeight: 1.2,
    fontFamily: "inherit",
    textDecoration: "none"
  };
  return <div style={{
    display: "flex",
    gap: "12px",
    margin: "16px 0",
    flexWrap: "wrap",
    alignItems: "center"
  }}>
      <button type="button" onClick={handleCopy} style={{
    ...baseButtonStyle,
    background: "transparent",
    color: "inherit",
    border: "1px solid rgba(127,127,127,0.4)"
  }}>
        Copier le prompt
      </button>
      <button type="button" onClick={handleBuild} style={{
    ...baseButtonStyle,
    background: "#F26207",
    color: "#FFFFFF",
    border: "1px solid #F26207"
  }}>
        Créer avec Replit ↗
      </button>
    </div>;
};

export const AiPrompt = ({children}) => {
  return <CodeBlock className="relative block max-w-full overflow-x-hidden font-sans !whitespace-pre-wrap !break-words" style={{
    overflowWrap: "anywhere",
    wordBreak: "break-word"
  }}>
      <div className="pr-7" style={{
    whiteSpace: "pre-wrap",
    overflowWrap: "anywhere",
    wordBreak: "break-word"
  }}>
        {children}
      </div>
    </CodeBlock>;
};

⏰ *Temps estimé : 15 minutes*

Un prototype est la version la plus simple d&#39;une idée de produit qu&#39;une personne peut essayer et sur laquelle elle peut réagir.

Vous utiliserez **Pace** comme exemple : un tracker de course à pied haut de gamme qui offre aux coureurs une vue d&#39;ensemble soignée de leurs entraînements, de leur récupération et de leurs progrès.

<Frame>
  <img src="/assets/mirror/pace-running-tracker-7c5b75e07856.png" alt="Le prototype Pace publié à une URL publique — un tableau de bord haut de gamme pour un tracker de course à pied avec la distance hebdomadaire, les courses récentes, les entraînements à venir et les réussites" width="2880" height="1800" data-path="images/build-examples/pace-running-tracker.png" />
</Frame>

<PromptActions prompt={PACE_PROMPT} campaign="docs-prototype-an-idea" />

<div id="o-que-voc-vai-aprender">
  ## Ce que vous allez apprendre
</div>

Vous apprendrez à :

* Transformer une idée de produit générale en un parcours testable.
* Utiliser le mode Plan pour définir le périmètre d’un prototype avant de le créer.
* Évaluer si le plan de l’Agent est suffisamment limité pour être testé.
* Utiliser les annotations dans Canvas et la collaboration pour recueillir des retours en contexte.
* Explorer en parallèle deux directions de prototype lorsque la problématique produit n’est pas claire.

<Steps>
  <Step title="Rédigez le prompt">
    Le prompt ci-dessus décrit **Pace** — un outil personnel de suivi de course à pied avec un tableau de bord d’accueil soigné, un parcours « Ajouter une course », des statistiques, une visualisation de plan provisoire et des données de démonstration réalistes de San Francisco. Utilisez **Copier le prompt** pour le coller vous-même dans Replit, ou **Créer dans Replit** pour ouvrir Replit avec le prompt déjà joint.

    Le prompt est volontairement précis sur la direction visuelle, les écrans, les données à simuler et ce qui est provisoire — c’est ce qui donne à l’Agent une cible claire à viser.
  </Step>

  <Step title="Utilisez le mode Plan (facultatif)">
    Si vous souhaitez qu&#39;Agent réfléchisse à la question produit avant d&#39;écrire la moindre ligne de code, activez **Plan** à côté du champ de saisie du prompt. Le mode Plan produit un plan structuré que vous pouvez examiner avant d&#39;approuver la construction — pratique lorsque la direction à prendre n&#39;est pas encore complètement arrêtée.

    <Frame>
      <img src="/assets/mirror/agent-plan-mode-prompt-059fe8d2c277.png" alt="Le compositeur de prompts de Replit avec le résumé du prototype Pace saisi et la case Plan mise en évidence, prêt à définir le périmètre de la construction avant qu'Agent ne démarre" width="1440" height="900" data-path="images/fitstart/work-with-agent/agent-plan-mode-prompt.png" />
    </Frame>

    Passez le plan en revue : utilisateur cible, flux principal, ce qui doit être réel, ce qui peut être simulé et une liste de vérification des tests. Si le plan prévoit trop de fonctionnalités, demandez à Agent de le réduire.

    <AiPrompt>
      Ce prototype est trop vaste.<br />
      Réduis-le à un utilisateur cible, un flux principal et une question à laquelle je peux répondre grâce aux retours.<br />
      Déplace les autres idées vers une liste d&#39;améliorations futures.
    </AiPrompt>
  </Step>

  <Step title="Passez en revue ce que l’Agent a créé">
    Une fois l’Agent terminé, ouvrez le Preview et utilisez Pace comme si vous le découvriez pour la première fois.

    Vérifiez que vous pouvez :

    * Voir le tableau de bord d’Alex Morgan avec des exemples de courses réalistes.
    * Enregistrer une nouvelle course à l’aide du bouton flottant **+** ou du bouton principal **Démarrer une course**.
    * Voir la course apparaître dans **Courses récentes** et les statistiques de **Cette semaine** se mettre à jour.
    * Naviguer entre les onglets Courses, Statistiques, Forfait et Profil.
    * Redimensionner le Preview à la largeur d’un téléphone pour vérifier la mise en page mobile.

    Si quelque chose semble incorrect, décrivez le symptôme à l’Agent et demandez la correction minimale.

    <Frame>
      <img src="/assets/mirror/fitstart-preview-1b631e7ed180.png" alt="Le prototype Pace en cours d’exécution dans le Preview — le workspace affiche le Chat de l’Agent à gauche et le tableau de bord Pace rempli à droite, c’est le type d’interface avec lequel interagirait une personne découvrant l’application pour la première fois" width="1440" height="900" data-path="images/fitstart/fitstart-preview.png" />
    </Frame>
  </Step>

  <Step title="Publiez et demandez des retours">
    Publiez dès que le parcours principal fonctionne.

    <Frame>
      <img src="/assets/mirror/fitstart-publish-dialog-137f98d0f7cd.png" alt="L'éditeur de projet avec les deux points d'entrée de publication visibles : la carte de publication intégrée dans le Chat de l'Agent avec un champ de sous-domaine et le bloc Publication dans le panneau Outils et fichiers" width="1440" height="900" data-path="images/fitstart/fitstart-publish-dialog.png" />
    </Frame>

    Une fois la publication effectuée, ouvrez l&#39;URL publique dans un nouvel onglet afin de vérifier que l&#39;expérience fonctionne bien pour les visiteurs qui la découvrent, avant de la partager.

    <Frame>
      <img src="/assets/mirror/pace-running-tracker-7c5b75e07856.png" alt="Le prototype Pace publié ouvert dans un nouvel onglet du navigateur, prêt à être partagé avec des relecteurs pour obtenir un retour ciblé" width="2880" height="1800" data-path="images/build-examples/pace-running-tracker.png" />
    </Frame>

    Partagez l&#39;URL avec une consigne précise :

    > Utilisez Pace comme un coureur qui cherche à suivre une semaine d&#39;entraînement. Enregistrez une course, parcourez le tableau de bord et dites-moi ce qui vous semble confus ou motivant.

    Un bon retour doit vous aider à décider quoi construire ensuite. Évitez de demander &quot;Qu&#39;en pensez-vous ?&quot;, sauf si vous tenez à obtenir des réponses vagues.
  </Step>

  <Step title="Collaborez avec votre équipe">
    Les prototypes de produit gagnent en qualité lorsque les coéquipiers peuvent examiner le même artefact et donner leur avis en contexte.

    Invitez des collaborateurs selon le degré de proximité avec lequel ils doivent travailler avec vous :

    * Invitez quelqu&#39;un sur le projet lorsqu&#39;il doit uniquement examiner ce prototype ou vous aider dessus. Voir [Inviter des coéquipiers](/fr/pt/build/invite-teammates).
    * Invitez quelqu&#39;un dans un workspace lorsqu&#39;il fait partie d&#39;une équipe permanente et a besoin d&#39;un accès à tous les projets. Voir [Workspaces d&#39;équipe](/fr/pt/features/collaboration/team-workspaces).

    <Frame>
      <img src="/assets/mirror/agent-invite-dialog-2703a335500c.png" alt="La boîte de dialogue Inviter ouverte dans la barre supérieure du workspace, avec le champ de saisie du nom d'utilisateur ou de l'e-mail mis en évidence, prêt à inviter un coéquipier sur le projet" width="700" height="400" data-path="images/fitstart/work-with-agent/agent-invite-dialog.png" />
    </Frame>

    Ensuite, utilisez le [Canvas](/fr/pt/design/canvas) lorsque le retour est visuel. Ajoutez des notes directement sur le prototype, annotez la zone que vous souhaitez modifier et demandez à Agent de s&#39;appuyer sur ces notes pour mettre à jour l&#39;app.

    <AiPrompt>
      Utilise les annotations du Canvas dans Pace pour améliorer le prototype.<br />
      Conserve le flux d&#39;enregistrement de course tel quel.<br />
      Applique les notes uniquement à la section hero, à la carte de progression hebdomadaire et à la liste des courses récentes.<br />
      Après la mise à jour, vérifie que l&#39;enregistrement d&#39;une course met toujours à jour la progression du jour.
    </AiPrompt>

    <Frame>
      <img src="/assets/mirror/agent-canvas-annotations-c88ab5856966.png" alt="Le frame Pace dans le Canvas avec un rectangle, une flèche et une étiquette de texte pointant vers un emplacement « bouton Se connecter avec Strava » à côté du bouton Enregistrer une course — le type de note contextuelle que les collaborateurs peuvent laisser à Agent" width="1440" height="900" data-path="images/fitstart/work-with-agent/agent-canvas-annotations.png" />
    </Frame>

    Pour les PM, cela signifie que le prototype fait office d&#39;artefact partagé : les designers peuvent annoter les flux, les ingénieurs peuvent en évaluer la faisabilité et les parties prenantes peuvent essayer la version publiée.
  </Step>
</Steps>

<div id="parabns">
  ## 🎉 Félicitations
</div>

Vous avez transformé une idée de produit en un prototype ciblé et partageable : un tableau de bord Pace fonctionnel que vos collègues et vos clients peuvent tester et commenter. Le plus difficile n&#39;était pas ce prototype, mais d&#39;apprendre à ramener une idée large à un parcours testable, à recueillir des retours en contexte et à décider ce qui mérite d&#39;être développé ensuite.

<div id="prximos-passos">
  ## Prochaines étapes
</div>

Utilisez les retours pour choisir la suite :

<CardGroup cols={2}>
  <Card title="Ajouter une base de données" icon="database" href="/fr/pt/build/add-database">
    Enregistrez l&#39;historique des courses pour le conserver après le rechargement.
  </Card>

  <Card title="Ajouter une connexion" icon="lock" href="/fr/pt/build/add-login">
    Donnez à chaque coureur un compte avec sa propre progression.
  </Card>

  <Card title="Créez une app mobile" icon="mobile-screen" href="/fr/pt/build/mobile-app">
    Emportez Pace avec vous pour suivre vos courses sur iOS ou Android.
  </Card>

  <Card title="Créez un tableau de bord à partir de données" icon="chart-line" href="/fr/pt/build/dashboard">
    Transformez l&#39;historique des courses en indicateurs de progression.
  </Card>
</CardGroup>

<div id="construa-em-paralelo">
  ### Construisez en parallèle
</div>

Quand la question produit porte sur la direction à prendre, et pas seulement sur la mise en œuvre, explorer plusieurs idées en même temps vous aide à comparer les options plutôt qu&#39;à vous engager trop tôt. Replit Agent prend en charge les tâches parallèles : vous pouvez demander à Agent de travailler sur plusieurs prototypes, fonctionnalités ou expérimentations à la fois, puis les examiner côte à côte.

<Frame>
  <img src="/assets/mirror/task-plan-sidebar-ecab81b06bb0.png" alt="Vue de fil montrant plusieurs tâches Agent exécutées en parallèle avec des indicateurs de statut" width="3430" height="1986" data-path="images/replitai/task-plan-sidebar.png" />
</Frame>

Pour découvrir comment les tâches sont mises en file d&#39;attente, exécutées et terminées, consultez [Système de tâches](/fr/pt/core-concepts/agent/task-system).

Lorsque vous hésitez sur la direction à prendre, demandez à Agent de créer deux tâches parallèles. Pour Pace, vous pourriez comparer :

* **Prototype axé sur la motivation** : séries, réussites, recommandations encourageantes.
* **Prototype axé sur l&#39;analyse** : statistiques plus approfondies, tendances d&#39;une semaine à l&#39;autre, records personnels.

<AiPrompt>
  Crée deux tâches parallèles pour améliorer Pace en tant que prototype de produit.<br />
  La tâche 1 doit explorer une version axée sur la motivation avec des séries, des réussites et des recommandations encourageantes.<br />
  La tâche 2 doit explorer une version axée sur l&#39;analyse avec des statistiques plus approfondies, des tendances d&#39;une semaine à l&#39;autre et des records personnels.<br />
  Garde les deux versions assez modestes pour être testées aujourd&#39;hui.<br />
  Pour chaque tâche, définis le parcours utilisateur, ce qu&#39;il faut construire et comment je dois évaluer le résultat.
</AiPrompt>

Une fois les deux tâches terminées, comparez-les à l&#39;aide de la même question :

> Quelle version vous donne le plus envie de revenir demain, et pourquoi ?

<div id="precisa-de-mais-ajuda">
  ## Besoin d’aide supplémentaire ?
</div>

* **Le prototype comporte trop de fonctionnalités :** demandez à Agent de le simplifier pour un utilisateur cible et un parcours.
* **Le public cible n’est pas clair :** demandez à Agent de réécrire la section de bienvenue pour un utilisateur cible.
* **Les données simulées masquent les risques :** demandez à Agent de distinguer ce qui est réel de ce qui est simulé.
* **Les retours sont vagues :** donnez aux évaluateurs une tâche et une question.
* **La publication échoue :** consultez la [Résolution des problèmes de publication](/fr/pt/build/troubleshooting).

<div id="relacionado">
  ## Ressources associées
</div>

* [Créer et publier votre première app](/fr/pt/build/your-first-app)
* [Vibe coding 101](/fr/pt/learn/foundations/vibe-coding-101)
* [Inviter des coéquipiers](/fr/pt/build/invite-teammates)