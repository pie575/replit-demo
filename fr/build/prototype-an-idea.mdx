---
title: "Prototyper une idée de produit"
description: "Transformez une idée de produit en un prototype ciblé, collaborez avec d’autres et utilisez les retours pour décider de la suite."
sidebarTitle: "Prototyper une idée"
icon: "lightbulb"
---

export const PACE_PROMPT = `Build me a personal running web app called "Pace".

I want a clean, modern web app where I can track my runs and see my progress at a glance — a friendlier, more visual version of a running journal like Strava or Nike Run Club, but simpler and prettier. It only needs to support one user (me) for now, and my profile name should be Alex Morgan. Pre-fill it with about a month of pretend running history so it looks alive from the moment I open it — runs through San Francisco, a mix of easy runs, tempo runs, long runs, and recovery runs, with at least 4–5 runs in the past week so the dashboard looks active.

The app should run in any modern browser, feel fast, and be fully responsive — great on desktop, tablet, and phone.

Look and feel
The vibe is sporty, premium, "athletic brand" — black, white, and a punchy lime green as the accent color. Use the Inter font everywhere. Rounded corners on everything (cards, buttons, pills). It should feel calm and uncluttered, not busy. Light mode by default. Smooth, subtle animations when pages transition, when cards appear, and when I click buttons — it should feel responsive and alive, not stiff.

Overall navigation
A top nav bar with the Pace logo on the left and 5 links across the middle: Home, Runs, Plan, Stats, Profile. The active link has a lime underline; inactive links are gray. On the right: a lime "Log a Run" button (always visible) and a round profile photo (clicking it opens Profile).

On smaller screens, the middle nav links collapse into a hamburger menu, but the lime "Log a Run" button stays visible.

The Home page (the main page) — a scrollable feed, top to bottom:

Greeting. A friendly greeting that changes with the time of day ("Good morning, Alex" / "Good afternoon, Alex" / "Good evening, Alex"), with a subtitle "Ready for your next run?".

Hero banner card. A wide photo of someone running on pavement, with a dark gradient on the left so the text is readable. Big white headline: "You don't find willpower, you create it." Underneath: "Keep showing up." Bottom-left: a lime pill button "Start a Run" with a little black play-circle. Clicking it opens the Log a Run dialog.

"This Week" stats. A row of four cards (2×2 grid on smaller widths): Distance (km), Time (h/min), Elevation (m), Runs (count). Each card: small icon, big number, "+12% vs last week" caption, thin squiggly line graph at the bottom (lime, blue, purple, lime). Numbers reflect my actual runs from the last 7 days.

Weekly Progress card. Title + small "View goals" link. Then "X of Y runs completed" and a big bold percentage on the right. Gradient lime progress bar. Below: seven day pills (M T W T F S S) with date numbers. Today is a black filled circle; days I've run show a lime circle with a white checkmark; others are empty circles.

Your Last Run card. A real interactive map (Leaflet, Mapbox, or similar) showing the route of my most recent run as a lime line, date in the corner, lime "View Run" button, and four stats (Distance, Time, Pace, Calories). Click → full run detail page.

Recent Runs section. Title "Recent Runs" + "See all" link. Three rows, one per recent run: tiny green map-line thumbnail on the left, run name + date, distance + pace lined up on the right. Click a row → run detail. A small "Delete" icon appears on hover.

Upcoming Workout card. Title + small lime circle icon with footprints. Workout name "Tempo Run", "Tomorrow · 6:30 AM". Three stats: 8 km, 5:40/km target pace, "Moderate" intensity. A muted gray "View workout" button. (Placeholder — no real training plan yet.)

Recent Achievements card. Two badges, each with a colorful icon, the badge name, and a one-line description. First is a real PR from my history (e.g. "10K Crusher — Completed a 10K in under 60 minutes"). Second is a placeholder: "Early Bird — Completed 5 runs before 7 AM" with a lime crescent moon icon.

The Runs page
Full list of all my runs, grouped by month (sticky month headers as I scroll). Each row like the recent-runs rows. A search input in the top-right filters by name or run type (Easy, Tempo, Long, Recovery, Interval). Click a row → run detail.

The Stats page — a scrollable view with:
- Time-range pill tabs at the top (7D / 30D / 3M / 6M / 1Y / All).
- Three KPI numbers (Total Distance, Avg Pace, Total Time), each with a green or red badge for change vs the previous period.
- A smooth lime area chart of distance over time. Hover a point to see that day's value.
- Below: a small bar chart of "runs per day of week" and a list of personal records (fastest 5K, fastest 10K, longest run, etc.).

The Plan page
A weekly calendar of upcoming workouts (placeholder — mostly the visual shell). Each day is a row showing what's planned (or "Rest day"). Today's row highlighted lime.

The Profile page
Avatar, name, weekly goal setting, total all-time stats (total distance, total runs, total hours), an "Upgrade to Pro" card with a lime sparkle and "Upgrade Now" button, and a settings list (units km/mi, notifications, sign out).

The "Log a Run" dialog (modal that centers on desktop, slides up from the bottom on mobile):
- Type of run (segmented control: Easy / Tempo / Long / Recovery / Interval)
- Distance in km (number input)
- Duration (mm:ss or hh:mm:ss input)
- Date and time (date and time pickers)
- Optional name
- Big lime "Save Run" button at the bottom
After saving: smooth dismiss, dashboard updates instantly, small toast confirmation.

The Run Detail page (a dedicated page when I click a run):
- Big interactive map of the full route at the top
- Date, run type chip, back link
- Stats row (Distance, Duration, Avg Pace, Calories)
- Per-kilometer splits with pace per km
- "..." menu with Edit and Delete

How it should behave
- The greeting changes based on time of day.
- Hero "Start a Run" and the nav "Log a Run" both open the same Log a Run dialog.
- Loading skeletons instead of blank space.
- Smooth page transitions.
- Fully responsive — every screen looks great on desktop, tablet, and phone.

Things to be honest about
- No real elevation data yet — use a sensible placeholder for the elevation stat card and flag it.
- The little route thumbnails in the recent-runs list can be a generic green squiggle if the real route shape would slow the list down — the big Run Detail map should be real.
- "Upcoming Workout" and the Plan page are placeholders — no training plan engine yet.
- No live GPS tracking in this first version — runs are added by manually filling the Log a Run form.

Build the whole thing end-to-end so it's actually usable: I should be able to open the URL, see my Alex Morgan dashboard fully populated with realistic San Francisco runs, log a new run from either the hero button or the nav, and have it show up in my stats and recent runs immediately.`;

export const PromptActions = ({prompt = "", campaign = "docs-prompt-actions"}) => {
  const LZ_SRC = "https://cdnjs.cloudflare.com/ajax/libs/lz-string/1.5.0/lz-string.min.js";
  const ensureLZString = () => {
    if (typeof document === "undefined") return;
    if (window.LZString) return;
    if (document.querySelector(`script[src="${LZ_SRC}"]`)) return;
    const s = document.createElement("script");
    s.src = LZ_SRC;
    s.async = true;
    document.head.appendChild(s);
  };
  const handleCopy = async e => {
    const btn = e.currentTarget;
    if (!prompt) return;
    try {
      await navigator.clipboard.writeText(prompt);
    } catch {
      const ta = document.createElement("textarea");
      ta.value = prompt;
      document.body.appendChild(ta);
      ta.select();
      try {
        document.execCommand("copy");
      } catch {}
      document.body.removeChild(ta);
    }
    const original = btn.dataset.label || btn.innerText;
    btn.dataset.label = original;
    btn.innerText = "Copié !";
    setTimeout(() => {
      if (btn.isConnected) btn.innerText = original;
    }, 1500);
  };
  const handleBuild = () => {
    if (!prompt || typeof window === "undefined") return;
    const utm = `utm_source=replit-docs&utm_medium=docs&utm_campaign=${encodeURIComponent(campaign)}&utm_content=prompt-actions`;
    let url;
    if (window.LZString) {
      const encoded = window.LZString.compressToEncodedURIComponent(prompt);
      url = `https://replit.com/?prompt=${encoded}&referrer=replit-docs&${utm}`;
    } else {
      url = `https://replit.com/?prompt=${encodeURIComponent(prompt)}&referrer=replit-docs&${utm}`;
    }
    window.open(url, "_blank", "noopener,noreferrer");
  };
  ensureLZString();
  const baseButtonStyle = {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    padding: "8px 16px",
    borderRadius: "8px",
    fontSize: "14px",
    fontWeight: 500,
    cursor: "pointer",
    lineHeight: 1.2,
    fontFamily: "inherit",
    textDecoration: "none"
  };
  return <div style={{
    display: "flex",
    gap: "12px",
    margin: "16px 0",
    flexWrap: "wrap",
    alignItems: "center"
  }}>
      <button type="button" onClick={handleCopy} style={{
    ...baseButtonStyle,
    background: "transparent",
    color: "inherit",
    border: "1px solid rgba(127,127,127,0.4)"
  }}>
        Copier le prompt
      </button>
      <button type="button" onClick={handleBuild} style={{
    ...baseButtonStyle,
    background: "#F26207",
    color: "#FFFFFF",
    border: "1px solid #F26207"
  }}>
        Créer sur Replit ↗
      </button>
    </div>;
};

export const AiPrompt = ({children}) => {
  return <CodeBlock className="relative block max-w-full overflow-x-hidden font-sans !whitespace-pre-wrap !break-words" style={{
    overflowWrap: "anywhere",
    wordBreak: "break-word"
  }}>
      <div className="pr-7" style={{
    whiteSpace: "pre-wrap",
    overflowWrap: "anywhere",
    wordBreak: "break-word"
  }}>
        {children}
      </div>
    </CodeBlock>;
};

⏰ *Durée estimée : 15 minutes*

Un prototype est la version la plus simple d’une idée de produit que l’on peut essayer et évaluer.

Vous utiliserez **Pace** comme exemple : un outil premium de suivi de course à pied qui offre aux coureurs une vue d’ensemble claire de leur entraînement, de leur récupération et de leurs progrès.

<Frame>
  <img src="/assets/mirror/pace-running-tracker-7c5b75e07856.png" alt="Le prototype Pace publié à une URL publique — un tableau de bord premium de suivi de course à pied présentant la distance hebdomadaire, les courses récentes, les entraînements à venir et les réussites" width="2880" height="1800" data-path="images/build-examples/pace-running-tracker.png" />
</Frame>

<PromptActions prompt={PACE_PROMPT} campaign="docs-prototype-an-idea" />

<div id="what-youll-learn">
  ## Ce que vous allez apprendre
</div>

Vous apprendrez à :

* Transformer une idée générale de produit en un parcours testable.
* Utiliser le mode Plan pour définir le périmètre d’un prototype avant de le créer.
* Vérifier si le plan d’Agent est suffisamment ciblé pour être testé.
* Utiliser les annotations dans Canvas et la collaboration pour recueillir des retours dans leur contexte.
* Explorer en parallèle deux pistes de prototype lorsque l’enjeu produit n’est pas clair.

<Steps>
  <Step title="Rédigez le prompt">
    Le prompt ci-dessus décrit **Pace** — un outil personnel de suivi de course à pied avec un tableau de bord d&#39;accueil soigné, un flux Ajouter une course, des statistiques, une vue de plan fictive et des données d&#39;exemple réalistes sur San Francisco. Utilisez **Copier le prompt** pour le coller vous-même dans Replit, ou **Build sur Replit** pour ouvrir Replit avec le prompt déjà joint.

    Le prompt est volontairement précis quant à la direction visuelle, aux écrans, aux données à simuler et à ce qui n&#39;est qu&#39;un espace réservé — c&#39;est ce qui donne à Agent une cible claire sur laquelle s&#39;appuyer pour évaluer le résultat.
  </Step>

  <Step title="Utiliser Plan Mode (facultatif)">
    Si vous souhaitez qu’Agent réfléchisse à la question produit avant d’écrire du code, activez **Plan** à côté du champ de saisie du prompt. Le mode Plan génère un plan structuré que vous pouvez examiner avant d’approuver le Build — utile lorsque l’orientation n’est pas encore totalement définie.

    <Frame>
      <img src="/assets/mirror/agent-plan-mode-prompt-059fe8d2c277.png" alt="Le rédacteur de prompts Replit avec le brief du prototype Pace saisi et la case Plan mise en évidence, prêt à définir le périmètre du Build avant qu’Agent ne commence" width="1440" height="900" data-path="images/fitstart/work-with-agent/agent-plan-mode-prompt.png" />
    </Frame>

    Examinez le plan : utilisateur cible, flux principal, éléments qui doivent être réels, éléments qui peuvent être simulés et liste de contrôle des tests. Si le plan ajoute trop de fonctionnalités, demandez à Agent de le simplifier.

    <AiPrompt>
      Ce prototype est trop vaste.<br />
      Réduisez-le à un utilisateur cible, un flux principal et une question à laquelle je peux répondre grâce aux retours.<br />
      Ajoutez les autres idées à une liste d’améliorations futures.
    </AiPrompt>
  </Step>

  <Step title="Examinez ce qu’Agent a créé">
    Lorsque Agent a terminé, ouvrez Preview et utilisez Pace comme si vous le découvriez pour la première fois.

    Vérifiez que vous pouvez :

    * Voir le tableau de bord d’Alex Morgan rempli d’exemples de courses réalistes.
    * Enregistrer une nouvelle course à l’aide du **+** flottant ou du bouton principal **Start a Run**.
    * Voir la course apparaître dans **Recent Runs** et les statistiques de **This Week** se mettre à jour.
    * Parcourir les onglets Runs, Stats, Plan et Profile.
    * Redimensionner Preview à la largeur d’un téléphone pour vérifier la disposition mobile.

    Si quelque chose ne semble pas fonctionner correctement, décrivez le problème à Agent et demandez-lui la correction la plus simple possible.

    <Frame>
      <img src="/assets/mirror/fitstart-preview-1b631e7ed180.png" alt="Le prototype Pace exécuté dans Preview — le workspace affiche le chat d’Agent à gauche et le tableau de bord Pace rempli à droite, soit l’interface avec laquelle une personne découvrant l’application pour la première fois interagirait" width="1440" height="900" data-path="images/fitstart/fitstart-preview.png" />
    </Frame>
  </Step>

  <Step title="Publier et demander des retours">
    Publiez lorsque le parcours principal fonctionne.

    <Frame>
      <img src="/assets/mirror/fitstart-publish-dialog-137f98d0f7cd.png" alt="Le Project Editor avec les deux options de publication visibles : la carte Publier intégrée au Chat Agent avec un champ de sous-domaine, et la vignette Publishing dans le panneau Outils et fichiers" width="1440" height="900" data-path="images/fitstart/fitstart-publish-dialog.png" />
    </Frame>

    Une fois l’application publiée, ouvrez l’URL publique dans un nouvel onglet de votre navigateur pour vérifier que l’expérience fonctionne pour les nouveaux visiteurs avant de la partager.

    <Frame>
      <img src="/assets/mirror/pace-running-tracker-7c5b75e07856.png" alt="Le prototype Pace publié, ouvert dans un nouvel onglet de navigateur et prêt à être partagé avec des évaluateurs pour recueillir des retours ciblés" width="2880" height="1800" data-path="images/build-examples/pace-running-tracker.png" />
    </Frame>

    Partagez l’URL en donnant une tâche précise :

    > Utilisez Pace comme un coureur qui souhaite suivre une semaine d’entraînement. Enregistrez une course, explorez le tableau de bord et dites-moi ce qui vous semble déroutant ou motivant.

    De bons retours devraient vous aider à décider quoi créer ensuite. Évitez de demander « Qu’en pensez-vous ? » à moins de vouloir des réponses vagues.
  </Step>

  <Step title="Collaborez avec votre équipe">
    Les prototypes de produit s&#39;améliorent lorsque les membres de l&#39;équipe peuvent examiner le même artifact et donner leur retour dans son contexte.

    Invitez des collaborateurs selon le degré de collaboration nécessaire :

    * Invitez une personne au projet si elle doit uniquement examiner ce prototype ou vous aider à le créer. Consultez [Inviter des membres de l&#39;équipe](/fr/build/invite-teammates).
    * Invitez une personne à un workspace si elle fait partie d&#39;une équipe pérenne et a besoin d&#39;accéder à plusieurs projets. Consultez [Workspaces d&#39;équipe](/fr/features/collaboration/team-workspaces).

    <Frame>
      <img src="/assets/mirror/agent-invite-dialog-2703a335500c.png" alt="Boîte de dialogue Inviter ouverte dans la barre supérieure du workspace, avec le champ de saisie du nom d'utilisateur ou de l'adresse e-mail en surbrillance, prête à inviter un membre de l'équipe au projet" width="700" height="400" data-path="images/fitstart/work-with-agent/agent-invite-dialog.png" />
    </Frame>

    Utilisez ensuite [Canvas](/fr/design/canvas) lorsque le retour porte sur l&#39;aspect visuel. Ajoutez des notes directement sur le prototype, annotez la zone à modifier et demandez à Agent d&#39;utiliser ces notes lors de la mise à jour de l&#39;app.

    <AiPrompt>
      Utilise les annotations Canvas sur Pace pour améliorer le prototype.<br />
      Ne modifie pas le flux d&#39;enregistrement des courses.<br />
      Applique les notes uniquement à la section principale, à la carte de progression hebdomadaire et à la liste des courses récentes.<br />
      Après la mise à jour, vérifie que l&#39;enregistrement d&#39;une course met toujours à jour la progression d&#39;aujourd&#39;hui.
    </AiPrompt>

    <Frame>
      <img src="/assets/mirror/agent-canvas-annotations-c88ab5856966.png" alt="Le frame Pace sur le Canvas, avec un rectangle, une flèche et une étiquette textuelle pointant vers l'emplacement d'un bouton « Se connecter avec Strava » à côté du bouton Log Run — le type de note contextuelle que les collaborateurs peuvent laisser à Agent" width="1440" height="900" data-path="images/fitstart/work-with-agent/agent-canvas-annotations.png" />
    </Frame>

    Pour les chefs de produit, cela signifie que le prototype sert d&#39;artifact partagé : les designers peuvent annoter les flux, les ingénieurs peuvent évaluer la faisabilité et les parties prenantes peuvent essayer la version publiée.
  </Step>
</Steps>

<div id="congratulations">
  ## 🎉 Félicitations
</div>

Vous avez transformé une idée de produit en un prototype ciblé et partageable : un tableau de bord Pace fonctionnel que vos collègues et clients peuvent essayer et commenter. Le plus difficile n&#39;était pas ce prototype, mais d&#39;apprendre à ramener une idée générale à un parcours testable, à recueillir des retours dans leur contexte et à déterminer ce qui mérite d&#39;être développé ensuite.

<div id="next-steps">
  ## Prochaines étapes
</div>

Utilisez les retours pour choisir la prochaine étape :

<CardGroup cols={2}>
  <Card title="Ajouter une base de données" icon="database" href="/fr/build/add-database">
    Enregistrez l’historique des courses afin de le conserver après actualisation.
  </Card>

  <Card title="Ajouter une connexion" icon="lock" href="/fr/build/add-login">
    Donnez à chaque coureur un compte avec sa propre progression.
  </Card>

  <Card title="Créer une application mobile" icon="mobile-screen" href="/fr/build/mobile-app">
    Adaptez Pace à un suivi sur mobile, sur iOS ou Android.
  </Card>

  <Card title="Créer un tableau de bord à partir de données" icon="chart-line" href="/fr/build/dashboard">
    Transformez l’historique des courses en indicateurs de progression.
  </Card>
</CardGroup>

<div id="build-in-parallel">
  ### Créer en parallèle
</div>

Lorsque les enjeux du produit vont au-delà de la simple implémentation, explorer plusieurs idées à la fois vous permet de comparer les options plutôt que de vous engager trop tôt. Replit Agent prend en charge les tâches parallèles : vous pouvez lui demander de travailler simultanément sur plusieurs prototypes, fonctionnalités ou expérimentations, puis de les examiner côte à côte.

<Frame>
  <img src="/assets/mirror/task-plan-sidebar-ecab81b06bb0.png" alt="Vue du fil de discussion montrant plusieurs tâches Agent exécutées en parallèle avec des indicateurs d’état" width="3430" height="1986" data-path="images/replitai/task-plan-sidebar.png" />
</Frame>

Pour savoir comment les tâches sont mises en file d’attente, exécutées et terminées, consultez le [système de tâches](/fr/core-concepts/agent/task-system).

Si vous hésitez sur la meilleure direction à prendre, demandez à Agent de créer deux tâches parallèles. Pour Pace, vous pourriez comparer :

* **Prototype axé sur la motivation** : séries, accomplissements, recommandations encourageantes.
* **Prototype axé sur l’analyse** : statistiques plus détaillées, tendances d’une semaine à l’autre, records personnels.

<AiPrompt>
  Crée deux tâches parallèles pour améliorer Pace en tant que prototype de produit.<br />
  La tâche 1 doit explorer une version axée sur la motivation avec des séries, des accomplissements et des recommandations encourageantes.<br />
  La tâche 2 doit explorer une version axée sur l’analyse avec des statistiques plus détaillées, des tendances d’une semaine à l’autre et des records personnels.<br />
  Garde les deux versions suffisamment simples pour pouvoir les tester aujourd’hui.<br />
  Pour chaque tâche, définis le parcours utilisateur, ce qu’il faut créer et comment je dois évaluer le résultat.
</AiPrompt>

Une fois les deux tâches terminées, comparez-les en posant la même question :

> Quelle version vous donne le plus envie de revenir demain, et pourquoi ?

<div id="need-further-help">
  ## Besoin d&#39;aide supplémentaire ?
</div>

* **Le prototype comporte trop de fonctionnalités :** demandez à Agent de le recentrer sur un seul utilisateur cible et un seul parcours.
* **Le public cible n&#39;est pas clair :** demandez à Agent de réécrire la section d&#39;accueil pour un seul utilisateur cible.
* **Les données fictives masquent les risques :** demandez à Agent d&#39;indiquer ce qui est réel et ce qui est simulé.
* **Les retours sont vagues :** donnez aux relecteurs une seule tâche et une seule question.
* **La publication échoue :** consultez [Résoudre les problèmes de publication](/fr/build/troubleshooting).

<div id="related">
  ## Articles associés
</div>

* [Créez et publiez votre première app](/fr/build/your-first-app)
* [Vibe coding 101](/fr/learn/foundations/vibe-coding-101)
* [Invitez vos coéquipiers](/fr/build/invite-teammates)