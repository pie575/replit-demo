---
title: "Open in Replit"
description: "Découvrez comment créer un bouton Open in Replit permettant aux visiteurs de commencer à créer une application directement depuis votre application ou votre site web."
---

export const CreateUrlLinkBuilder = () => {
  if (typeof document !== 'undefined' && !window.LZString) {
    const script = document.createElement('script');
    script.src = 'https://cdnjs.cloudflare.com/ajax/libs/lz-string/1.5.0/lz-string.min.js';
    document.head.appendChild(script);
  }
  const getLink = () => {
    if (typeof document === 'undefined' || !window.LZString) {
      return 'https://replit.com/?stack=Design&prompt=';
    }
    const promptEl = document.getElementById('create-url-prompt');
    const stackEl = document.querySelector('input[name="stack-mode"]:checked');
    const referrerEl = document.getElementById('create-url-referrer');
    const prompt = promptEl?.value || '';
    const stack = stackEl?.value || 'Design';
    const referrer = referrerEl?.value || '';
    if (!prompt.trim()) {
      return '';
    }
    const encoded = window.LZString.compressToEncodedURIComponent(prompt);
    let url = `https://replit.com/?stack=${stack}&prompt=${encoded}`;
    if (referrer.trim()) {
      url += `&referrer=${encodeURIComponent(referrer)}`;
    }
    return url;
  };
  const updateOutputs = () => {
    if (typeof document === 'undefined') return;
    const link = getLink();
    const badgeUrl = 'https://replit.com/badge?caption=Build%20with%20Replit';
    const emptyPlaceholder = 'Enter a prompt above to generate a link';
    const badgeMarkdown = link ? `[![Build with Replit](${badgeUrl})](${link})` : emptyPlaceholder;
    const outputEl = document.getElementById('create-url-output');
    const badgeMarkdownEl = document.getElementById('create-url-badge-markdown');
    if (outputEl) outputEl.textContent = link || emptyPlaceholder;
    if (badgeMarkdownEl) badgeMarkdownEl.textContent = badgeMarkdown;
  };
  const handleBadgeClick = e => {
    e.preventDefault();
    const link = getLink();
    if (link) {
      window.open(link, '_blank');
    }
  };
  return <div className="space-y-4">
      <div>
        <label htmlFor="create-url-prompt" className="block text-sm font-medium mb-1">
          Prompt
        </label>
        <textarea id="create-url-prompt" onInput={updateOutputs} placeholder="Create a todo app with a clean, modern design" rows={4} className="w-full px-3 py-2 border border-gray-300 dark:border-gray-700 rounded-md bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100" />
      </div>
      <div>
        <label className="block text-sm font-medium mb-2">Stack mode</label>
        <div className="flex gap-4">
          <label className="flex items-center cursor-pointer">
            <input type="radio" name="stack-mode" value="Design" defaultChecked onChange={updateOutputs} className="mr-2" />
            <span className="text-sm">Design</span>
          </label>
          <label className="flex items-center cursor-pointer">
            <input type="radio" name="stack-mode" value="Build" onChange={updateOutputs} className="mr-2" />
            <span className="text-sm">Build</span>
          </label>
        </div>
      </div>
      <div>
        <label htmlFor="create-url-referrer" className="block text-sm font-medium mb-1">
          Referrer <span className="text-gray-500 font-normal">(optional)</span>
        </label>
        <input id="create-url-referrer" type="text" onInput={updateOutputs} placeholder="Your website or app name" className="w-full px-3 py-2 border border-gray-300 dark:border-gray-700 rounded-md bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100" />
        <p className="mt-1 text-xs text-gray-500">
          The name or URL of the page containing this link. Used for
          attribution.
        </p>
      </div>
      <div>
        <label className="block text-sm font-medium mb-1">Generated link</label>
        <pre className="w-full p-3 bg-gray-100 dark:bg-gray-800 rounded-md text-sm break-all select-all cursor-text">
          <code id="create-url-output">
            https://replit.com/?stack=Design&amp;prompt=
          </code>
        </pre>
      </div>
      <div>
        <label className="block text-sm font-medium mb-1">Badge preview</label>
        <div className="p-3 bg-gray-100 dark:bg-gray-800 rounded-md">
          <a href="#" onClick={handleBadgeClick} style={{
    cursor: 'pointer'
  }}>
            <img src="https://replit.com/badge?caption=Open%20in%20Replit" alt="Open in Replit badge" noZoom />
          </a>
        </div>
      </div>
      <div>
        <label className="block text-sm font-medium mb-1">Badge markdown</label>
        <pre className="w-full p-3 bg-gray-100 dark:bg-gray-800 rounded-md text-sm break-all select-all cursor-text">
          <code id="create-url-badge-markdown">
            [![Open in
            Replit](https://replit.com/badge?caption=Open%20in%20Replit)](https://replit.com/?stack=Design&amp;prompt=)
          </code>
        </pre>
      </div>
    </div>;
};

<Frame caption="Cliquez sur ce badge pour ouvrir automatiquement Replit avec le prompt et les paramètres déjà renseignés.">
  <div style={{ padding: '2rem', display: 'flex', justifyContent: 'center' }}>
    <a href="https://replit.com/?stack=Build&prompt=A4Jw9gtsAuQ&referrer=replit-docs" target="_blank">
      <img src="https://replit.com/badge?caption=Open%20in%20Replit" alt="Open in Replit" noZoom />
    </a>
  </div>
</Frame>

<div id="section">
  ## Créer des liens
</div>

Pour créer un lien, utilisez le format suivant.

```
https://replit.com/?stack=Build&prompt=A4Jw9gtsAuQ&referrer=replit-docs
```

<div id="url">
  ### Paramètres d’URL
</div>

Veillez à inclure les paramètres suivants dans l’URL.

| Paramètre  | Type   | Description                                                                                                                                                                                                                                    | Obligatoire/Facultatif |
| ---------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| `stack`    | string | Le mode Build à utiliser. Il doit s’agir de `Design` ou de `Build`. `Design` ouvre le [Canvas](/fr/ko/design/canvas) pour le prototypage visuel. `Build` ouvre [Agent](/fr/ko/features/agent/overview) pour le développement complet d’applications. | Obligatoire            |
| `prompt`   | string | Texte décrivant l’application à créer, **compressé avec [LZ-string](https://pieroxy.net/blog/pages/lz-string/index.html)**. Un prompt non compressé peut contenir jusqu’à 50 000 caractères.                                                   | Obligatoire            |
| `referrer` | string | Nom ou URL de la page contenant ce lien. Utilisé à des fins d’attribution.                                                                                                                                                                     | Facultatif             |

<Note>
  Le paramètre `prompt` doit être compressé avec LZ-string avant d’être ajouté à l’URL.
  Cela permet de maintenir l’URL dans les limites de longueur du navigateur et de l’analyser
  correctement.
</Note>

<div id="section-2">
  ### Constructeur de liens conversationnel
</div>

Utilisez l’outil ci-dessous pour générer un lien **Open in Replit**. Saisissez un prompt et sélectionnez le mode de stack : un lien compressé avec LZ-string sera généré automatiquement.

<CreateUrlLinkBuilder />

<div id="section-3">
  ### Compresser le prompt
</div>

Pour créer un lien **Open in Replit** valide, vous devez compresser le prompt à l’aide de LZ-string. Voici comment :

<div id="javascripttypescript">
  #### JavaScript/TypeScript
</div>

```javascript theme={null}
import LZString from 'lz-string';

const prompt = 'Create a todo app with a clean, modern design';
const compressed = LZString.compressToEncodedURIComponent(prompt);
const url = `https://replit.com/?stack=Design&prompt=${compressed}`;
```

<div id="python">
  #### Python
</div>

```python theme={null}
import lzstring

prompt = "Create a todo app with a clean, modern design"
compressed = lzstring.LZString().compressToEncodedURIComponent(prompt)
url = f"https://replit.com/?stack=Design&prompt={compressed}"
```

<Tip>
  Utilisez `compressToEncodedURIComponent` pour que la chaîne compressée soit compatible avec les URL.
</Tip>

<div id="section-4">
  ### Exemple
</div>

**Canvas avec un prompt de base**

```
https://replit.com/?stack=Design&prompt=AoUwTgzg9gdghgGwAQAcpgC4DMoIJZRIR4YgB0QA
```

**Mode Build avec des prompts détaillés**

```
https://replit.com/?stack=Build&prompt=&referrer=replit%20docs
```

<div id="section-5">
  ## Mode Stack
</div>

<div id="canvas">
  ### Canvas
</div>

Utilisez `stack=Design` pour ouvrir des prompts dans [Canvas](/fr/ko/design/canvas). Il est optimisé pour :

* la conception visuelle et le prototypage d’UI
* l’itération rapide sur les mises en page et les styles
* la création de maquettes
* le développement orienté frontend

<div id="build-mode">
  ### Mode Build
</div>

Ouvrez un prompt dans [Agent](/fr/ko/features/agent/overview) avec `stack=Build`. Ce mode est optimisé pour :

* le développement d&#39;applications full stack
* la logique back-end et les API
* l&#39;intégration de bases de données
* des fonctionnalités complexes

<div id="section-6">
  ## Bonnes pratiques
</div>

* Pour obtenir les meilleurs résultats, rédigez des prompts concis et concentrez-vous sur les fonctionnalités essentielles.
* Utilisez un langage qui décrit clairement ce que vous souhaitez créer.
* Choisissez le mode `stack` adapté à votre cas d&#39;utilisation (Design pour l&#39;UI/UX, Build pour l&#39;application complète).
* Vérifiez que l&#39;URL raccourcie est correctement analysée avant de la partager.
* Tenez compte du niveau technique de votre public cible lors de la rédaction des prompts.

<div id="section-7">
  ### Authentification des utilisateurs
</div>

Quel que soit votre état d’authentification sur Replit, vous devez fournir une entrée avant de créer une application. Cela implique généralement d’envoyer un prompt.

<div id="section-8">
  ### Gestion des erreurs
</div>

Si le format de l’URL est incorrect ou si les paramètres ne sont pas valides, Replit ne préremplit pas le prompt. Les causes possibles sont les suivantes :

* Paramètre requis manquant (`stack` ou `prompt`)
* Valeur `stack` non valide (doit être `Design` ou `Build`)
* Échec de la décompression du paramètre `prompt`
* Erreur « URL too long » du navigateur (bien que la compression puisse contribuer à l’atténuer)

<div id="section-9">
  ### Considérations de sécurité
</div>

<Warning>
  Les liens **Open in Replit** peuvent servir à créer des applications au comportement inattendu ou présentant des vulnérabilités de sécurité. Cliquez uniquement sur des liens provenant de sources fiables.
</Warning>

Lorsque vous partagez des liens **Open in Replit** :

* examinez attentivement le prompt avant de le partager publiquement
* tenez compte des implications de sécurité de l&#39;application générée
* n&#39;incluez pas d&#39;informations sensibles dans le prompt
* indiquez clairement le contenu que le lien générera

<div id="section-10">
  ## Ressources associées
</div>

* [Documentation Canvas](/fr/ko/design/canvas)
* [Documentation Agent](/fr/ko/features/agent/overview)
* [Intégrations IA de Replit](/fr/ko/features/integrations/overview)