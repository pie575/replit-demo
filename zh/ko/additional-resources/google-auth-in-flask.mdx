---
title: "在 Python 和 Flask 中使用 Google 身份验证"
description: "了解如何在 Replit 上的 Flask 应用中实现 Google OAuth 身份验证，包括用户登录和 Google Sheets API 集成。"
---

允许用户通过 Google 账号登录网站有以下好处：

1. 无需自行实现身份验证机制。
2. 可轻松获取用户的姓名和联系信息。
3. 可使用相同的凭据访问用户的 Google 资源，例如 Sheets 和 Drive。

本指南将介绍如何在 Replit 上使用 Python 和 Flask 实现这一功能。

首先，我们将了解如何设置基本的 OAuth 身份验证，然后学习如何使用身份验证后获得的凭据访问用户的 Google 资源。

<div id="oauth">
  ## OAuth 简介
</div>

Google 身份验证基于 OAuth 标准。OAuth 的工作原理如下：

1. 网站会在某个环节将用户引导至登录页面。
2. 用户进入登录页面后，网站不会自行实现登录表单，而是将用户重定向到 Google 的登录服务进行登录。
3. Google 的登录服务成功验证用户身份后，会将用户重定向回网站，并将与用户及登录会话相关的信息发送到预先定义的 URL (例如 `https://YOUR_DOMAIN/oauth2callback`) 。
4. 使用用户的登录信息进一步获取访问令牌。此令牌类似于通行证，可用于访问用户的资源，例如个人资料信息、电子表格和文档等。

<div id="oauth-2">
  ## OAuth：查看代码
</div>

如果您想先看代码，下面就是所需的代码。不过，要让一切正常运行，您需要先在 Google Cloud Console 中进行一些设置。下一节将介绍这些设置。使用 [Flask 模板](https://replit.com/@replit/Flask?v=1)创建一个新的 Replit 应用，并将以下代码粘贴到 `main.py` 中。代码中的注释说明了各部分的作用：

```python theme={null}
from flask import Flask, redirect, session, url_for, request
import google_auth_oauthlib.flow
import json
import os
import requests

app = Flask('app')
# `FLASK_SECRET_KEY` 用于会话。请生成一个随机字符串，
# 并将其保存为 Secrets。
app.secret_key = os.environ.get('FLASK_SECRET_KEY') or os.urandom(24)

# `GOOGLE_APIS_OAUTH_SECRET` 包含从 Google Cloud 凭据面板下载的
# JSON 文件内容。请参阅下一节。
oauth_config = json.loads(os.environ['GOOGLE_OAUTH_SECRETS'])

# 配置 OAuth 流程
oauth_flow = google_auth_oauthlib.flow.Flow.from_client_config(
    oauth_config,
    # scopes 定义认证后可代表用户访问的 API
    scopes=[
        "https://www.googleapis.com/auth/userinfo.email",
        "openid",
        "https://www.googleapis.com/auth/userinfo.profile",
    ]
)

# 这是登录页面的入口点，会重定向到 `authorization_url` 指向的 Google 登录服务。
# `redirect_uri` 是 Google 登录服务重定向回此 App 时使用的 URI。
@app.route('/signin')
def signin():
    # Replit App 内部使用 http，但外部访问使用 https，
    # 因此将 URL 从 http 转换为 https，以与 redirect_uri 保持一致
    oauth_flow.redirect_uri = url_for('oauth2callback', _external=True).replace('http://', 'https://')
    authorization_url, state = oauth_flow.authorization_url()
    session['state'] = state
    return redirect(authorization_url)

# 这是 Google 登录服务重定向回来的端点。您需要在 Google Cloud 的
# API 凭据面板中将其添加到“Authorized redirect URIs”。
# 它会调用 Google 端点请求访问令牌，并将其存储在用户会话中。
# 随后即可使用此访问令牌代表用户访问 API。
@app.route('/oauth2callback')
def oauth2callback():
    if not session['state'] == request.args['state']:
        return 'Invalid state parameter', 400
    oauth_flow.fetch_token(authorization_response=request.url.replace('http:', 'https:'))
    session['access_token'] = oauth_flow.credentials.token
    return redirect("/")

# 这是 App 的主页。若用户尚未登录，则提示其登录；
# 若已登录，则显示用户信息。
@app.route('/')
def welcome():
    if "access_token" in session:
        user_info = get_user_info(session["access_token"])
        if user_info:
            return f"""
                Hello {user_info["given_name"]}!<br>
                Your email address is {user_info["email"]}<br>
                <a href="/logout">Log out</a>
            """
    return """
        <h1>Hello!</h1>
        <a href="/signin">Sign In via Google</a><br>
    """

# 使用有效的访问令牌调用 userinfo API，以获取用户信息。
# 这是使用访问令牌代表用户访问 API 的第一个示例。
def get_user_info(access_token):
    response = requests.get("https://www.googleapis.com/oauth2/v3/userinfo", headers={
       "Authorization": f"Bearer {access_token}"
   })
    if response.status_code == 200:
        user_info = response.json()
        return user_info
    else:
        print(f"Failed to fetch user info: {response.status_code} {response.text}")
        return None

@app.route('/logout')
def logout():
    session.clear()
    return redirect('/')

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)

```

<div id="oauth-3">
  ## OAuth 应用 / 客户端设置
</div>

要使上述代码正常运行，您需要在 Google Cloud 中完成以下操作：

1. 创建 Google Cloud 项目 (如果尚未创建) 。
2. 配置 OAuth 同意屏幕。
3. 为您的应用创建 OAuth 客户端 ID。

<div id="google-cloud">
  ### 创建 Google Cloud 项目
</div>

如果您已有可用于此操作的 Google Cloud 项目，可以跳过此步骤。

1. 前往 [Google Cloud Console](https://console.cloud.google.com/)。
2. 点击 Google Cloud 徽标旁的项目选择器下拉菜单：

<Frame>
  <img src="/assets/mirror/google-project-dropdown-d64bd797ae82.png" alt="Google 项目下拉菜单" width="734" height="94" data-path="images/google-auth-in-flask/google-project-dropdown.png" />
</Frame>

3. 选择现有项目，或点击“New Project”创建新项目。

<Frame>
  <img src="/assets/mirror/select-project-22c9b948c348.png" alt="选择项目" width="1092" height="868" data-path="images/google-auth-in-flask/select-project.png" />
</Frame>

4. 如果要创建新项目，请输入项目名称，然后点击“Create”。

<Frame>
  <img src="/assets/mirror/new-project-8a60880bda21.png" alt="新项目" width="1110" height="868" data-path="images/google-auth-in-flask/new-project.png" />
</Frame>

新项目出现在弹出窗口后，点击“Select project”将其设为*当前项目*。

<div id="oauth-4">
  ### 配置 OAuth 同意屏幕
</div>

创建项目后，您可以配置 OAuth 同意屏幕：

1. 前往 [OAuth 同意屏幕](https://console.cloud.google.com/apis/credentials/consent)。

<Frame>
  <img src="/assets/mirror/oauth-consent-screen-8c5d7264f4ba.png" alt="OAuth 同意屏幕" width="1196" height="675" data-path="images/google-auth-in-flask/oauth-consent-screen.png" />
</Frame>

2. 确认项目下拉菜单中已选中所需的项目。
3. 选择 &quot;External&quot;，让所有拥有 Google 账号的用户都能登录您的应用。&quot;Internal&quot; 则仅允许组织内的人员登录。
4. 点击 &quot;Create&quot;。
5. 输入应用名称，以及负责支持此应用的联系人的电子邮件地址。

<Frame>
  <img src="/assets/mirror/oauth-consent-step-2-f00ee976e9fd.png" alt="OAuth 同意屏幕" width="1196" height="675" data-path="images/google-auth-in-flask/oauth-consent-step-2.png" />
</Frame>

6. 在 &quot;Developer contact information&quot; 下输入电子邮件地址。

<Frame>
  <img src="/assets/mirror/oauth-consent-step-2-b-a2f1a259f431.png" alt="开发者联系信息" width="533" height="207" data-path="images/google-auth-in-flask/oauth-consent-step-2-b.png" />
</Frame>

7. 点击 &quot;Save and continue&quot;。
8. 在 Scopes 页面，您可以添加应用需要访问的 API。应用已获准访问用于获取基本用户信息的 API。

<Frame>
  <img src="/assets/mirror/oauth-scopes-f99364e44221.png" alt="OAuth 范围" width="1093" height="647" data-path="images/google-auth-in-flask/oauth-scopes.png" />
</Frame>

暂时保持默认设置，然后点击 &quot;Save and continue&quot;。
9. 在 Test Users 中，添加将在测试期间测试应用的用户的电子邮件地址。

<Frame>
  <img src="/assets/mirror/test-users-048f029f4d9f.png" alt="测试用户" width="1050" height="641" data-path="images/google-auth-in-flask/test-users.png" />
</Frame>

点击 &quot;Add users&quot;。
10. 添加一个或多个 Google 电子邮件地址，然后点击 &quot;Add&quot;。

<Frame>
  <img src="/assets/mirror/test-users-add-43e7efabac17.png" alt="测试用户" width="1050" height="641" data-path="images/google-auth-in-flask/test-users-add.png" />
</Frame>

然后点击 &quot;Save and continue&quot;。
11. 查看摘要页面。您可以随时返回并编辑各个步骤。

<Frame>
  <img src="/assets/mirror/summary-54322ee205dd.png" alt="测试用户" width="735" height="328" data-path="images/google-auth-in-flask/summary.png" />
</Frame>

<div id="oauth-id">
  ### 创建应用的 OAuth 客户端 ID
</div>

这是最后一步。要让 OAuth 正常运行，您需要为应用创建 OAuth 客户端 ID。

1. 前往 [Credentials](https://console.cloud.google.com/apis/credentials)。

<Frame>
  <img src="/assets/mirror/credentials-37967178ae47.png" alt="测试用户" width="1050" height="605" data-path="images/google-auth-in-flask/credentials.png" />
</Frame>

2. 点击“Create credentials”。

<Frame>
  <img src="/assets/mirror/create-credentials-411c8b243090.png" alt="测试用户" width="1050" height="605" data-path="images/google-auth-in-flask/create-credentials.png" />
</Frame>

选择“OAuth client ID”。
3. 在 Application type 中选择“Web application”，然后为该客户端 ID 输入名称。

<Frame>
  <img src="/assets/mirror/create-credentials-web-53cd170ef6ff.png" alt="测试用户" width="1050" height="605" data-path="images/google-auth-in-flask/create-credentials-web.png" />
</Frame>

4. 现在回到 Flask Replit 应用。打开 Shell 并输入：`echo https://$REPLIT_DEV_DOMAIN/oauth2callback`。输出结果应类似于：`https://81309e9b-c4df-48e0-a2c2-0a8d3c0e3162-00-35ppsa0tcuv6v.infra-staging.replit.dev/oauth2callback`。复制这段文本，并将其粘贴到表单底部的“Authorized redirect URIs”中。

<Frame>
  <img src="/assets/mirror/authorized-redirect-uris-6a5811acd49a.png" alt="测试用户" width="800" height="271" data-path="images/google-auth-in-flask/authorized-redirect-uris.png" />
</Frame>

稍后发布应用时，您还需要添加 `https://YOUR_APP_DOMAIN/oauth2callback`。

5. 点击“Create”。
6. 点击“Download JSON”：

<Frame>
  <img src="/assets/mirror/secrets_json-d507ecb4ea34.png" alt="测试用户" width="573" height="607" data-path="images/google-auth-in-flask/secrets_json.png" />
</Frame>

7. 回到 Replit 应用并打开 Secrets 面板。创建一个名为 `GOOGLE_OAUTH_SECRETS` 的密钥，将下载文件的内容
   粘贴为密钥值。

<Frame>
  <img src="/assets/mirror/add-secret-92f7d85d5156.png" alt="测试用户" width="1060" height="677" data-path="images/google-auth-in-flask/add-secret.png" />
</Frame>

做得好！如果您已完成以上步骤，恭喜您！现在可以运行 Flask 应用，并使用测试用户的 Google 账号登录。若要让所有 Google 用户都能使用您的应用，请返回[同意屏幕页面](https://console.cloud.google.com/apis/credentials/consent)，然后点击“Publish App”。如果需要使用 Sheets、Drive 等其他 Google API，可能还需完成验证流程。

接下来将介绍如何集成 Sheets 等 Google API。想继续深入了解的话，请继续阅读。

<div id="google-sheets-api">
  ## 设置 Google Sheets API
</div>

要添加 Google Sheets 等 Google API 集成，首先需要为您的应用启用相应的 API。您可以[浏览可用的 API](https://console.cloud.google.com/apis/library)。以下将以 Google Sheets 为例。

1. 前往 [Google Sheets API 列表页面](https://console.cloud.google.com/apis/library/sheets.googleapis.com)。
2. 点击“启用”。

完成！Google Cloud 这部分的设置到此结束。

<div id="google-sheets">
  ## Google Sheets 集成：查看代码
</div>

首先，保持原始代码中 OAuth 流程部分不变，只需在权限范围列表中添加 `"https://www.googleapis.com/auth/spreadsheets.readonly"`：

```python theme={null}
# 配置 OAuth 流程
oauth_flow = google_auth_oauthlib.flow.Flow.from_client_config(
    oauth_config,
    # scopes 定义了认证后可代表用户访问的 API
    scopes=[
        "https://www.googleapis.com/auth/userinfo.email",
        "openid",
        "https://www.googleapis.com/auth/userinfo.profile",
        "https://www.googleapis.com/auth/spreadsheets.readonly"
    ]
)
```

现在，使用 `googleapiclient.discovery` 库访问 Google API 时，先用访问令牌创建 `Credentials` 对象，再使用 `build` 函数创建可调用的 API 对象。以 Sheets API 为例：

```python theme={null}
credentials = google.oauth2.credentials.Credentials(token=session['access_token'])
service = build("sheets", "v4", credentials=credentials)
sheets_api = service.spreadsheets()
```

我们创建了一些辅助函数，用于实际调用 Sheets API：

```python theme={null}
# 获取 Google 电子表格中的所有工作表
def get_sheets(sheets_api, spreadsheet_id) -> list[str]:
    result = sheets_api.get(spreadsheetId=spreadsheet_id).execute()
    return [sheet["properties"]["title"] for sheet in result["sheets"]]

# 获取 Google 电子表格中指定工作表的数据
def get_sheet_data(sheets_api, spreadsheet_id, sheet_title) -> list[list[str]]:
    result = (
        sheets_api.values()
        .get(spreadsheetId=spreadsheet_id, range=sheet_title)
        .execute()
    )
    return result["values"]
```

借助以上内容，您可以按如下方式创建一个用于导入 Google 电子表格的 POST 处理程序端点：

```python theme={null}
@app.route("/import_spreadsheet", methods = ['POST'])
def import_spreadsheet():
    if 'access_token' not in session:
        return redirect('/signin')
    spreadsheet_id = request.form["spreadsheet_id"]
    credentials = google.oauth2.credentials.Credentials(token=session['access_token'])
    service = build("sheets", "v4", credentials=credentials)
    sheets_api = service.spreadsheets()
    try:
        sheets = get_sheets(sheets_api, spreadsheet_id)
        data_by_sheets = {}
        for sheet in sheets:
            data = get_sheet_data(sheets_api, spreadsheet_id, sheet)
            data_by_sheets[sheet] = data
    except googleapiclient.errors.HttpError as e:
        return f"upload failure"
    dirpath = os.path.join("static", "uploads", spreadsheet_id)
    filepath = os.path.join(dirpath, "data.json")
    os.makedirs(dirpath, exist_ok=True)
    with open(filepath, "w") as file:
        json.dump(data_by_sheets, file)
    return "upload success!"
```

完整代码如下：

```python theme={null}
from flask import Flask, redirect, session, url_for, request
import google_auth_oauthlib.flow
import json
import os
import requests
from googleapiclient.discovery import build
import googleapiclient.errors
import google.oauth2.credentials

app = Flask('app')
# `FLASK_SECRET_KEY` 用于会话。请生成一个随机字符串，
# 并将其存储到 Secrets 中。
app.secret_key = os.environ.get('FLASK_SECRET_KEY') or os.urandom(24)

# `GOOGLE_APIS_OAUTH_SECRET` 包含从 Google Cloud Credentials 面板下载的
# JSON 文件内容。请参阅下一节。
oauth_config = json.loads(os.environ['GOOGLE_OAUTH_SECRETS'])

# 配置 OAuth 流程
oauth_flow = google_auth_oauthlib.flow.Flow.from_client_config(
    oauth_config,
    # scopes 定义了完成认证后可代表用户访问的 API
    scopes=[
        "https://www.googleapis.com/auth/userinfo.email",
        "openid",
        "https://www.googleapis.com/auth/userinfo.profile",
        "https://www.googleapis.com/auth/spreadsheets.readonly"
    ]
)

# 这是登录页面的入口，会重定向到 `authorization_url` 中指定的 Google 登录服务。
# `redirect_uri` 是 Google 登录服务重定向回此 App 时使用的 URI。
@app.route('/signin')
def signin():
    # Replit App 内部使用 http，外部访问则使用 https，
    # 因此将 URL 从 http 转换为 https，以确保 redirect_uri 与之匹配
    oauth_flow.redirect_uri = url_for('oauth2callback', _external=True).replace('http://', 'https://')
    authorization_url, state = oauth_flow.authorization_url()
    session['state'] = state
    return redirect(authorization_url)

# 这是 Google 登录服务重定向回来的端点。必须将其添加到 Google Cloud
# API 凭据面板的 "Authorized redirect URIs" 中。
# 它会调用 Google 端点请求访问令牌，并将其存储在用户会话中。
# 此后可使用该访问令牌代表用户访问 API。
@app.route('/oauth2callback')
def oauth2callback():
    if not session['state'] == request.args['state']:
        return 'Invalid state parameter', 400
    oauth_flow.fetch_token(authorization_response=request.url.replace('http:', 'https:'))
    session['access_token'] = oauth_flow.credentials.token
    return redirect("/")

# 使用有效的访问令牌调用 userinfo API，获取用户信息。
# 这是使用访问令牌代表用户访问 API 的第一个示例。
def get_user_info(access_token):
    response = requests.get("https://www.googleapis.com/oauth2/v3/userinfo", headers={
       "Authorization": f"Bearer {access_token}"
   })
    if response.status_code == 200:
        user_info = response.json()
        return user_info
    else:
        print(f"Failed to fetch user info: {response.status_code} {response.text}")
        return None

@app.route('/logout')
def logout():
    session.clear()
    return redirect('/')

# 获取 Google 电子表格中的所有工作表
def get_sheets(sheets_api, spreadsheet_id) -> list[str]:
    result = sheets_api.get(spreadsheetId=spreadsheet_id).execute()
    return [sheet["properties"]["title"] for sheet in result["sheets"]]

# 获取 Google 电子表格中指定工作表的数据
def get_sheet_data(sheets_api, spreadsheet_id, sheet_title) -> list[list[str]]:
    result = (
        sheets_api.values()
        .get(spreadsheetId=spreadsheet_id, range=sheet_title)
        .execute()
    )
    return result["values"]

# 渲染电子表格导入表单
@app.route("/import_spreadsheet_form")
def import_spreadsheet_form():
    return """
    <h3>Import Spreadsheet</h3>
    <form action="/import_spreadsheet" method="POST">
        <label>Spreadsheet ID</label>
        <input type="text" name="spreadsheet_id">

        <button type="submit">Import</button>
    </form>
    """

@app.route("/import_spreadsheet", methods = ['POST'])
def import_spreadsheet():
    if 'access_token' not in session:
        return redirect('/signin')
    spreadsheet_id = request.form["spreadsheet_id"]
    credentials = google.oauth2.credentials.Credentials(token=session['access_token'])
    service = build("sheets", "v4", credentials=credentials)
    sheets_api = service.spreadsheets()
    try:
        sheets = get_sheets(sheets_api, spreadsheet_id)
        data_by_sheets = {}
        for sheet in sheets:
            data = get_sheet_data(sheets_api, spreadsheet_id, sheet)
            data_by_sheets[sheet] = data
    except googleapiclient.errors.HttpError as e:
        return f"upload failure"
    dirpath = os.path.join("static", "uploads", spreadsheet_id)
    filepath = os.path.join(dirpath, "data.json")
    os.makedirs(dirpath, exist_ok=True)
    with open(filepath, "w") as file:
        json.dump(data_by_sheets, file)
    return "upload success! Really!"

@app.route('/')
def welcome():
    if "access_token" in session:
        user_info = get_user_info(session["access_token"])
        if user_info:
            return f"""
            Hello {user_info["given_name"]}!<br>
            Your email address is {user_info["email"]}<br>
            <a href="/signin">Sign In to Google</a><br>
            <a href="/import_spreadsheet_form">Import a Sheet</a>
            """
    return """
    <h1>Welcome to Google Sheet Importer</h1>
    <a href="/signin">Sign In to Google</a><br>
    <a href="/import_spreadsheet_form">Import a Sheet</a>
    """

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
```

发布 App 时，请务必：

1. 在“Authorized redirect URIs”中添加生产环境的 `/oauth2callback` URI。
2. 前往[同意屏幕页面](https://console.cloud.google.com/apis/credentials/consent)，然后点击“Publish App”。

希望您使用愉快，祝您接下来的旅程一切顺利。