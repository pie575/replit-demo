---
title: "Прототипирование продуктовой идеи"
description: "Превратите идею продукта в компактный прототип, работайте над ним вместе с другими и на основе обратной связи решайте, что создавать дальше."
---

export const PACE_PROMPT = `Build me a personal running web app called "Pace".

I want a clean, modern web app where I can track my runs and see my progress at a glance — a friendlier, more visual version of a running journal like Strava or Nike Run Club, but simpler and prettier. It only needs to support one user (me) for now, and my profile name should be Alex Morgan. Pre-fill it with about a month of pretend running history so it looks alive from the moment I open it — runs through San Francisco, a mix of easy runs, tempo runs, long runs, and recovery runs, with at least 4–5 runs in the past week so the dashboard looks active.

The app should run in any modern browser, feel fast, and be fully responsive — great on desktop, tablet, and phone.

Look and feel
The vibe is sporty, premium, "athletic brand" — black, white, and a punchy lime green as the accent color. Use the Inter font everywhere. Rounded corners on everything (cards, buttons, pills). It should feel calm and uncluttered, not busy. Light mode by default. Smooth, subtle animations when pages transition, when cards appear, and when I click buttons — it should feel responsive and alive, not stiff.

Overall navigation
A top nav bar with the Pace logo on the left and 5 links across the middle: Home, Runs, Plan, Stats, Profile. The active link has a lime underline; inactive links are gray. On the right: a lime "Log a Run" button (always visible) and a round profile photo (clicking it opens Profile).

On smaller screens, the middle nav links collapse into a hamburger menu, but the lime "Log a Run" button stays visible.

The Home page (the main page) — a scrollable feed, top to bottom:

Greeting. A friendly greeting that changes with the time of day ("Good morning, Alex" / "Good afternoon, Alex" / "Good evening, Alex"), with a subtitle "Ready for your next run?".

Hero banner card. A wide photo of someone running on pavement, with a dark gradient on the left so the text is readable. Big white headline: "You don't find willpower, you create it." Underneath: "Keep showing up." Bottom-left: a lime pill button "Start a Run" with a little black play-circle. Clicking it opens the Log a Run dialog.

"This Week" stats. A row of four cards (2×2 grid on smaller widths): Distance (km), Time (h/min), Elevation (m), Runs (count). Each card: small icon, big number, "+12% vs last week" caption, thin squiggly line graph at the bottom (lime, blue, purple, lime). Numbers reflect my actual runs from the last 7 days.

Weekly Progress card. Title + small "View goals" link. Then "X of Y runs completed" and a big bold percentage on the right. Gradient lime progress bar. Below: seven day pills (M T W T F S S) with date numbers. Today is a black filled circle; days I've run show a lime circle with a white checkmark; others are empty circles.

Your Last Run card. A real interactive map (Leaflet, Mapbox, or similar) showing the route of my most recent run as a lime line, date in the corner, lime "View Run" button, and four stats (Distance, Time, Pace, Calories). Click → full run detail page.

Recent Runs section. Title "Recent Runs" + "See all" link. Three rows, one per recent run: tiny green map-line thumbnail on the left, run name + date, distance + pace lined up on the right. Click a row → run detail. A small "Delete" icon appears on hover.

Upcoming Workout card. Title + small lime circle icon with footprints. Workout name "Tempo Run", "Tomorrow · 6:30 AM". Three stats: 8 km, 5:40/km target pace, "Moderate" intensity. A muted gray "View workout" button. (Placeholder — no real training plan yet.)

Recent Achievements card. Two badges, each with a colorful icon, the badge name, and a one-line description. First is a real PR from my history (e.g. "10K Crusher — Completed a 10K in under 60 minutes"). Second is a placeholder: "Early Bird — Completed 5 runs before 7 AM" with a lime crescent moon icon.

The Runs page
Full list of all my runs, grouped by month (sticky month headers as I scroll). Each row like the recent-runs rows. A search input in the top-right filters by name or run type (Easy, Tempo, Long, Recovery, Interval). Click a row → run detail.

The Stats page — a scrollable view with:
- Time-range pill tabs at the top (7D / 30D / 3M / 6M / 1Y / All).
- Three KPI numbers (Total Distance, Avg Pace, Total Time), each with a green or red badge for change vs the previous period.
- A smooth lime area chart of distance over time. Hover a point to see that day's value.
- Below: a small bar chart of "runs per day of week" and a list of personal records (fastest 5K, fastest 10K, longest run, etc.).

The Plan page
A weekly calendar of upcoming workouts (placeholder — mostly the visual shell). Each day is a row showing what's planned (or "Rest day"). Today's row highlighted lime.

The Profile page
Avatar, name, weekly goal setting, total all-time stats (total distance, total runs, total hours), an "Upgrade to Pro" card with a lime sparkle and "Upgrade Now" button, and a settings list (units km/mi, notifications, sign out).

The "Log a Run" dialog (modal that centers on desktop, slides up from the bottom on mobile):
- Type of run (segmented control: Easy / Tempo / Long / Recovery / Interval)
- Distance in km (number input)
- Duration (mm:ss or hh:mm:ss input)
- Date and time (date and time pickers)
- Optional name
- Big lime "Save Run" button at the bottom
After saving: smooth dismiss, dashboard updates instantly, small toast confirmation.

The Run Detail page (a dedicated page when I click a run):
- Big interactive map of the full route at the top
- Date, run type chip, back link
- Stats row (Distance, Duration, Avg Pace, Calories)
- Per-kilometer splits with pace per km
- "..." menu with Edit and Delete

How it should behave
- The greeting changes based on time of day.
- Hero "Start a Run" and the nav "Log a Run" both open the same Log a Run dialog.
- Loading skeletons instead of blank space.
- Smooth page transitions.
- Fully responsive — every screen looks great on desktop, tablet, and phone.

Things to be honest about
- No real elevation data yet — use a sensible placeholder for the elevation stat card and flag it.
- The little route thumbnails in the recent-runs list can be a generic green squiggle if the real route shape would slow the list down — the big Run Detail map should be real.
- "Upcoming Workout" and the Plan page are placeholders — no training plan engine yet.
- No live GPS tracking in this first version — runs are added by manually filling the Log a Run form.

Build the whole thing end-to-end so it's actually usable: I should be able to open the URL, see my Alex Morgan dashboard fully populated with realistic San Francisco runs, log a new run from either the hero button or the nav, and have it show up in my stats and recent runs immediately.`;

export const PromptActions = ({prompt = "", campaign = "docs-prompt-actions"}) => {
  const LZ_SRC = "https://cdnjs.cloudflare.com/ajax/libs/lz-string/1.5.0/lz-string.min.js";
  const ensureLZString = () => {
    if (typeof document === "undefined") return;
    if (window.LZString) return;
    if (document.querySelector(`script[src="${LZ_SRC}"]`)) return;
    const s = document.createElement("script");
    s.src = LZ_SRC;
    s.async = true;
    document.head.appendChild(s);
  };
  const handleCopy = async e => {
    const btn = e.currentTarget;
    if (!prompt) return;
    try {
      await navigator.clipboard.writeText(prompt);
    } catch {
      const ta = document.createElement("textarea");
      ta.value = prompt;
      document.body.appendChild(ta);
      ta.select();
      try {
        document.execCommand("copy");
      } catch {}
      document.body.removeChild(ta);
    }
    const original = btn.dataset.label || btn.innerText;
    btn.dataset.label = original;
    btn.innerText = "Скопировано!";
    setTimeout(() => {
      if (btn.isConnected) btn.innerText = original;
    }, 1500);
  };
  const handleBuild = () => {
    if (!prompt || typeof window === "undefined") return;
    const utm = `utm_source=replit-docs&utm_medium=docs&utm_campaign=${encodeURIComponent(campaign)}&utm_content=prompt-actions`;
    let url;
    if (window.LZString) {
      const encoded = window.LZString.compressToEncodedURIComponent(prompt);
      url = `https://replit.com/?prompt=${encoded}&referrer=replit-docs&${utm}`;
    } else {
      url = `https://replit.com/?prompt=${encodeURIComponent(prompt)}&referrer=replit-docs&${utm}`;
    }
    window.open(url, "_blank", "noopener,noreferrer");
  };
  ensureLZString();
  const baseButtonStyle = {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    padding: "8px 16px",
    borderRadius: "8px",
    fontSize: "14px",
    fontWeight: 500,
    cursor: "pointer",
    lineHeight: 1.2,
    fontFamily: "inherit",
    textDecoration: "none"
  };
  return <div style={{
    display: "flex",
    gap: "12px",
    margin: "16px 0",
    flexWrap: "wrap",
    alignItems: "center"
  }}>
      <button type="button" onClick={handleCopy} style={{
    ...baseButtonStyle,
    background: "transparent",
    color: "inherit",
    border: "1px solid rgba(127,127,127,0.4)"
  }}>
        Скопировать промпт
      </button>
      <button type="button" onClick={handleBuild} style={{
    ...baseButtonStyle,
    background: "#F26207",
    color: "#FFFFFF",
    border: "1px solid #F26207"
  }}>
        Создать в Replit ↗
      </button>
    </div>;
};

export const AiPrompt = ({children}) => {
  return <CodeBlock className="relative block max-w-full overflow-x-hidden font-sans !whitespace-pre-wrap !break-words" style={{
    overflowWrap: "anywhere",
    wordBreak: "break-word"
  }}>
      <div className="pr-7" style={{
    whiteSpace: "pre-wrap",
    overflowWrap: "anywhere",
    wordBreak: "break-word"
  }}>
        {children}
      </div>
    </CodeBlock>;
};

⏰ *Время: около 15 минут*

Прототип — это минимальная версия идеи продукта, которую можно опробовать и оставить обратную связь.

В качестве примера используем **Pace** — премиальный трекер для бега, который помогает бегунам удобно отслеживать тренировки, восстановление и прогресс.

<Frame>
  <img src="/assets/mirror/pace-running-tracker-7c5b75e07856.png" alt="Прототип Pace, опубликованный по публичному URL — панель премиального трекера для бега с еженедельным километражем, недавними пробежками, предстоящими тренировками и достижениями" width="2880" height="1800" data-path="images/build-examples/pace-running-tracker.png" />
</Frame>

<PromptActions prompt={PACE_PROMPT} campaign="docs-prototype-an-idea" />

<div id="section">
  ## Что вы узнаете
</div>

Вы узнаете, как:

* Превращать общую продуктовую идею в один тестируемый сценарий.
* Использовать режим Plan, чтобы определить рамки прототипа до начала разработки.
* Проверять, достаточно ли план Agent компактен для тестирования.
* Использовать аннотации и совместную работу в Canvas для сбора обратной связи непосредственно в контексте.
* Параллельно исследовать два направления прототипа, когда продуктовый вопрос сформулирован недостаточно ясно.

<Steps>
  <Step title="Написание промптов">
    В промпте выше описан **Pace** — персональный трекер пробежек с продуманной главной панелью, сценарием Add Run, статистикой, представлением планов с заполнителями и реалистичными примерами данных из Сан-Франциско. Используйте **Скопировать промпт**, чтобы вставить его в Replit самостоятельно, или нажмите **Создать в Replit**, чтобы открыть Replit с уже прикреплённым промптом.

    В промпте намеренно подробно описаны визуальный стиль, экраны, данные для имитации и заполнители — это даёт Agent чёткую цель для оценки.
  </Step>

  <Step title="Использовать режим Plan (необязательно)">
    Если вы хотите, чтобы Agent помог продумать продуктовые вопросы до написания кода, включите **Plan** рядом с полем ввода промпта. В режиме Plan создаётся структурированный план, который можно просмотреть перед тем, как одобрить сборку. Это удобно, если вы ещё не до конца определились с направлением.

    <Frame>
      <img src="/assets/mirror/agent-plan-mode-prompt-059fe8d2c277.png" alt="Поле ввода промпта Replit с описанием прототипа Pace и выделенным флажком Plan, позволяющим определить объём работ до того, как Agent начнёт сборку" width="1440" height="900" data-path="images/fitstart/work-with-agent/agent-plan-mode-prompt.png" />
    </Frame>

    Просмотрите план: целевой пользователь, основной сценарий, что нужно сделать реалистичным, что можно смоделировать, и чек-лист тестирования. Если в плане слишком много функций, попросите Agent сократить его.

    <AiPrompt>
      Этот прототип слишком масштабный.<br />
      Сократи его до одного целевого пользователя, одного основного сценария и одного вопроса, на который я смогу получить ответ с помощью обратной связи.<br />
      Перенеси остальные идеи в список будущих улучшений.
    </AiPrompt>
  </Step>

  <Step title="Проверить то, что создал Agent">
    Когда Agent завершит работу, откройте Preview и попробуйте пользоваться Pace как пользователь, который зашёл впервые.

    Проверьте следующее:

    * Отображается панель управления Alex Morgan с реалистичными примерами пробежек.
    * Можно записать новую пробежку с помощью плавающей кнопки **+** или кнопки **Start a Run** в главном блоке.
    * Пробежки отображаются в разделе **Recent Runs**, а статистика в **This Week** обновляется.
    * Можно просматривать вкладки Runs, Stats, Plan и Profile.
    * Уменьшите ширину Preview до размера экрана телефона, чтобы проверить мобильную раскладку.

    Если что-то работает не так, опишите проблему Agent и попросите внести минимальные исправления.

    <Frame>
      <img src="/assets/mirror/fitstart-preview-1b631e7ed180.png" alt="Прототип Pace в Preview: слева в рабочем пространстве показан чат с Agent, а справа — заполненная панель управления Pace, которой можно пользоваться как при первом посещении" width="1440" height="900" data-path="images/fitstart/fitstart-preview.png" />
    </Frame>
  </Step>

  <Step title="Опубликуйте приложение и запросите обратную связь">
    Когда основной сценарий будет работать, опубликуйте приложение.

    <Frame>
      <img src="/assets/mirror/fitstart-publish-dialog-137f98d0f7cd.png" alt="Два способа опубликовать приложение: встроенная карточка Publish с полем для поддомена в чате Agent и плитка Publishing на панели «Инструменты и файлы» в Project Editor" width="1440" height="900" data-path="images/fitstart/fitstart-publish-dialog.png" />
    </Frame>

    После публикации откройте публичный URL в новой вкладке браузера и перед тем, как делиться приложением, убедитесь, что оно корректно работает для нового пользователя.

    <Frame>
      <img src="/assets/mirror/pace-running-tracker-7c5b75e07856.png" alt="Опубликованный прототип Pace, открытый в новой вкладке браузера и готовый к отправке рецензентам для получения целенаправленной обратной связи" width="2880" height="1800" data-path="images/build-examples/pace-running-tracker.png" />
    </Frame>

    Поделитесь URL и предложите одну конкретную задачу:

    > Используйте Pace как бегун, который хочет записать неделю тренировок. Запишите одну пробежку и просмотрите панель управления, затем расскажите, что вызвало затруднения или мотивировало вас.

    Качественная обратная связь помогает решить, что создавать в Build дальше. Вопрос «Что вы думаете?» приводит к расплывчатым ответам.
  </Step>

  <Step title="Совместная работа с командой">
    Прототипы продуктов становятся лучше, когда участники команды могут вместе просматривать один и тот же результат и оставлять обратную связь в контексте.

    Приглашайте соавторов в зависимости от того, насколько тесно им нужно работать с этим прототипом:

    * Если им нужно только просмотреть этот прототип или помочь с ним, пригласите их в проект. См. [Приглашение участников команды](/ru/ja/build/invite-teammates).
    * Если они входят в постоянную команду и нуждаются в доступе к нескольким проектам, пригласите их в Workspace. См. [Командные рабочие пространства](/ru/ja/features/collaboration/team-workspaces).

    <Frame>
      <img src="/assets/mirror/agent-invite-dialog-2703a335500c.png" alt="Диалог приглашения в верхней панели рабочего пространства. Выделено поле для ввода имени пользователя или адреса электронной почты — можно приглашать участников команды в проект" width="700" height="400" data-path="images/fitstart/work-with-agent/agent-invite-dialog.png" />
    </Frame>

    Если обратная связь касается визуальной части, используйте [Canvas](/ru/ja/design/canvas). Добавляйте заметки прямо к прототипу, аннотируйте элементы, которые хотите изменить, и попросите Agent учесть эти заметки при обновлении приложения.

    <AiPrompt>
      Используй аннотации Canvas для Pace, чтобы улучшить прототип.<br />
      Не изменяй процесс логирования пробежек.<br />
      Примени заметки только к главному разделу, карточке еженедельного прогресса и списку последних пробежек.<br />
      После обновления проверь, что логирование пробежки по-прежнему обновляет сегодняшний прогресс.
    </AiPrompt>

    <Frame>
      <img src="/assets/mirror/agent-canvas-annotations-c88ab5856966.png" alt="Фрейм Pace в Canvas. Прямоугольник, стрелка и текстовая метка указывают на область «Log in with Strava button» рядом с кнопкой Log Run — пример заметки с контекстом, которую соавтор может оставить для Agent" width="1440" height="900" data-path="images/fitstart/work-with-agent/agent-canvas-annotations.png" />
    </Frame>

    Для PM это означает, что прототип становится общим рабочим артефактом: дизайнеры могут аннотировать пользовательские потоки, инженеры — оценивать реализуемость, а заинтересованные стороны — тестировать опубликованную версию.
  </Step>
</Steps>

<div id="section-2">
  ## 🎉 Поздравляем
</div>

Вы превратили идею продукта в сфокусированный прототип, которым можно поделиться, — работающий дашборд Pace, который участники команды и клиенты могут протестировать и оставить обратную связь. Сложность была не в создании этого прототипа. Главное — научиться сужать общую идею до одного тестируемого сценария, собирать обратную связь в контексте и решать, что стоит создавать дальше.

<div id="section-3">
  ## Следующие шаги
</div>

Выберите один из следующих шагов:

<CardGroup cols={2}>
  <Card title="Добавить базу данных" icon="database" href="/ru/ja/build/add-database">
    Сохраняйте историю пробежек даже после обновления страницы.
  </Card>

  <Card title="Добавить вход" icon="lock" href="/ru/ja/build/add-login">
    Предоставьте каждому бегуну аккаунт для отслеживания собственного прогресса.
  </Card>

  <Card title="Создать мобильное приложение" icon="mobile-screen" href="/ru/ja/build/mobile-app">
    Добавьте в Pace поддержку отслеживания пробежек на iOS и Android с приоритетом мобильных устройств.
  </Card>

  <Card title="Создать дашборд из данных" icon="chart-line" href="/ru/ja/build/dashboard">
    Превратите историю пробежек в аналитику прогресса.
  </Card>
</CardGroup>

<div id="section-4">
  ### Параллельная разработка
</div>

Если вопрос о продукте касается не реализации, а выбора направления, параллельное изучение нескольких идей позволяет сравнить варианты, не принимая решение слишком рано. Replit Agent поддерживает параллельные задачи — поручите Agent одновременно проработать несколько прототипов, функций или экспериментов и сравните их.

<Frame>
  <img src="/assets/mirror/task-plan-sidebar-ecab81b06bb0.png" alt="Представление ветки, в котором несколько задач Agent выполняются параллельно с индикаторами статуса" width="3430" height="1986" data-path="images/replitai/task-plan-sidebar.png" />
</Frame>

Подробнее о том, как задачи ставятся в очередь, выполняются и завершаются, см. в разделе [Система задач](/ru/ja/core-concepts/agent/task-system).

Если вы не уверены, какое направление лучше, попросите Agent создать две параллельные задачи. Для Pace можно сравнить:

* **Прототип с упором на мотивацию**: серии, достижения и мотивирующие рекомендации.
* **Прототип с упором на аналитику**: подробная статистика, недельные тренды и личные рекорды.

<AiPrompt>
  Создай две параллельные задачи для улучшения Pace как прототипа продукта.<br />
  В задаче 1 проработай версию с упором на мотивацию: серии, достижения и мотивирующие рекомендации.<br />
  В задаче 2 проработай версию с упором на аналитику: подробная статистика, тренды неделя к неделе и личные рекорды.<br />
  Обе версии должны быть достаточно небольшими, чтобы протестировать их уже сегодня.<br />
  Для каждой задачи определи пользовательский путь, что нужно создать и как мне оценить результат.
</AiPrompt>

Когда обе задачи будут завершены, сравните их, задав один и тот же вопрос для обратной связи:

> Какую версию вам больше захочется использовать завтра? Почему?

<div id="section-5">
  ## Нужна дополнительная помощь?
</div>

* **В прототипе слишком много функций:** Попросите Agent сосредоточиться на одном целевом пользователе и одном сценарии.
* **Целевая аудитория неясна:** Попросите Agent переписать приветственный раздел для одного целевого пользователя.
* **Моковые данные скрывают риски:** Попросите Agent перечислить, что реально, а что является моковыми данными.
* **Обратная связь слишком расплывчата:** Дайте проверяющему одну задачу и один вопрос.
* **Не удаётся опубликовать приложение:** Воспользуйтесь [руководством по устранению неполадок при публикации](/ru/ja/build/troubleshooting).

<div id="section-6">
  ## Связанные материалы
</div>

* [Создание и публикация первого приложения](/ru/ja/build/your-first-app)
* [Введение в вайб-кодинг](/ru/ja/learn/foundations/vibe-coding-101)
* [Приглашение участников команды](/ru/ja/build/invite-teammates)