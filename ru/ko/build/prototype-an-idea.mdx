---
title: "Создание прототипа продукта"
description: "Превратите идею продукта в проработанный прототип, сотрудничайте с другими и используйте обратную связь, чтобы решить, что создавать дальше."
---

export const PACE_PROMPT = `Build me a personal running web app called "Pace".

I want a clean, modern web app where I can track my runs and see my progress at a glance — a friendlier, more visual version of a running journal like Strava or Nike Run Club, but simpler and prettier. It only needs to support one user (me) for now, and my profile name should be Alex Morgan. Pre-fill it with about a month of pretend running history so it looks alive from the moment I open it — runs through San Francisco, a mix of easy runs, tempo runs, long runs, and recovery runs, with at least 4–5 runs in the past week so the dashboard looks active.

The app should run in any modern browser, feel fast, and be fully responsive — great on desktop, tablet, and phone.

Look and feel
The vibe is sporty, premium, "athletic brand" — black, white, and a punchy lime green as the accent color. Use the Inter font everywhere. Rounded corners on everything (cards, buttons, pills). It should feel calm and uncluttered, not busy. Light mode by default. Smooth, subtle animations when pages transition, when cards appear, and when I click buttons — it should feel responsive and alive, not stiff.

Overall navigation
A top nav bar with the Pace logo on the left and 5 links across the middle: Home, Runs, Plan, Stats, Profile. The active link has a lime underline; inactive links are gray. On the right: a lime "Log a Run" button (always visible) and a round profile photo (clicking it opens Profile).

On smaller screens, the middle nav links collapse into a hamburger menu, but the lime "Log a Run" button stays visible.

The Home page (the main page) — a scrollable feed, top to bottom:

Greeting. A friendly greeting that changes with the time of day ("Good morning, Alex" / "Good afternoon, Alex" / "Good evening, Alex"), with a subtitle "Ready for your next run?".

Hero banner card. A wide photo of someone running on pavement, with a dark gradient on the left so the text is readable. Big white headline: "You don't find willpower, you create it." Underneath: "Keep showing up." Bottom-left: a lime pill button "Start a Run" with a little black play-circle. Clicking it opens the Log a Run dialog.

"This Week" stats. A row of four cards (2×2 grid on smaller widths): Distance (km), Time (h/min), Elevation (m), Runs (count). Each card: small icon, big number, "+12% vs last week" caption, thin squiggly line graph at the bottom (lime, blue, purple, lime). Numbers reflect my actual runs from the last 7 days.

Weekly Progress card. Title + small "View goals" link. Then "X of Y runs completed" and a big bold percentage on the right. Gradient lime progress bar. Below: seven day pills (M T W T F S S) with date numbers. Today is a black filled circle; days I've run show a lime circle with a white checkmark; others are empty circles.

Your Last Run card. A real interactive map (Leaflet, Mapbox, or similar) showing the route of my most recent run as a lime line, date in the corner, lime "View Run" button, and four stats (Distance, Time, Pace, Calories). Click → full run detail page.

Recent Runs section. Title "Recent Runs" + "See all" link. Three rows, one per recent run: tiny green map-line thumbnail on the left, run name + date, distance + pace lined up on the right. Click a row → run detail. A small "Delete" icon appears on hover.

Upcoming Workout card. Title + small lime circle icon with footprints. Workout name "Tempo Run", "Tomorrow · 6:30 AM". Three stats: 8 km, 5:40/km target pace, "Moderate" intensity. A muted gray "View workout" button. (Placeholder — no real training plan yet.)

Recent Achievements card. Two badges, each with a colorful icon, the badge name, and a one-line description. First is a real PR from my history (e.g. "10K Crusher — Completed a 10K in under 60 minutes"). Second is a placeholder: "Early Bird — Completed 5 runs before 7 AM" with a lime crescent moon icon.

The Runs page
Full list of all my runs, grouped by month (sticky month headers as I scroll). Each row like the recent-runs rows. A search input in the top-right filters by name or run type (Easy, Tempo, Long, Recovery, Interval). Click a row → run detail.

The Stats page — a scrollable view with:
- Time-range pill tabs at the top (7D / 30D / 3M / 6M / 1Y / All).
- Three KPI numbers (Total Distance, Avg Pace, Total Time), each with a green or red badge for change vs the previous period.
- A smooth lime area chart of distance over time. Hover a point to see that day's value.
- Below: a small bar chart of "runs per day of week" and a list of personal records (fastest 5K, fastest 10K, longest run, etc.).

The Plan page
A weekly calendar of upcoming workouts (placeholder — mostly the visual shell). Each day is a row showing what's planned (or "Rest day"). Today's row highlighted lime.

The Profile page
Avatar, name, weekly goal setting, total all-time stats (total distance, total runs, total hours), an "Upgrade to Pro" card with a lime sparkle and "Upgrade Now" button, and a settings list (units km/mi, notifications, sign out).

The "Log a Run" dialog (modal that centers on desktop, slides up from the bottom on mobile):
- Type of run (segmented control: Easy / Tempo / Long / Recovery / Interval)
- Distance in km (number input)
- Duration (mm:ss or hh:mm:ss input)
- Date and time (date and time pickers)
- Optional name
- Big lime "Save Run" button at the bottom
After saving: smooth dismiss, dashboard updates instantly, small toast confirmation.

The Run Detail page (a dedicated page when I click a run):
- Big interactive map of the full route at the top
- Date, run type chip, back link
- Stats row (Distance, Duration, Avg Pace, Calories)
- Per-kilometer splits with pace per km
- "..." menu with Edit and Delete

How it should behave
- The greeting changes based on time of day.
- Hero "Start a Run" and the nav "Log a Run" both open the same Log a Run dialog.
- Loading skeletons instead of blank space.
- Smooth page transitions.
- Fully responsive — every screen looks great on desktop, tablet, and phone.

Things to be honest about
- No real elevation data yet — use a sensible placeholder for the elevation stat card and flag it.
- The little route thumbnails in the recent-runs list can be a generic green squiggle if the real route shape would slow the list down — the big Run Detail map should be real.
- "Upcoming Workout" and the Plan page are placeholders — no training plan engine yet.
- No live GPS tracking in this first version — runs are added by manually filling the Log a Run form.

Build the whole thing end-to-end so it's actually usable: I should be able to open the URL, see my Alex Morgan dashboard fully populated with realistic San Francisco runs, log a new run from either the hero button or the nav, and have it show up in my stats and recent runs immediately.`;

export const PromptActions = ({prompt = "", campaign = "docs-prompt-actions"}) => {
  const LZ_SRC = "https://cdnjs.cloudflare.com/ajax/libs/lz-string/1.5.0/lz-string.min.js";
  const ensureLZString = () => {
    if (typeof document === "undefined") return;
    if (window.LZString) return;
    if (document.querySelector(`script[src="${LZ_SRC}"]`)) return;
    const s = document.createElement("script");
    s.src = LZ_SRC;
    s.async = true;
    document.head.appendChild(s);
  };
  const handleCopy = async e => {
    const btn = e.currentTarget;
    if (!prompt) return;
    try {
      await navigator.clipboard.writeText(prompt);
    } catch {
      const ta = document.createElement("textarea");
      ta.value = prompt;
      document.body.appendChild(ta);
      ta.select();
      try {
        document.execCommand("copy");
      } catch {}
      document.body.removeChild(ta);
    }
    const original = btn.dataset.label || btn.innerText;
    btn.dataset.label = original;
    btn.innerText = "Скопировано!";
    setTimeout(() => {
      if (btn.isConnected) btn.innerText = original;
    }, 1500);
  };
  const handleBuild = () => {
    if (!prompt || typeof window === "undefined") return;
    const utm = `utm_source=replit-docs&utm_medium=docs&utm_campaign=${encodeURIComponent(campaign)}&utm_content=prompt-actions`;
    let url;
    if (window.LZString) {
      const encoded = window.LZString.compressToEncodedURIComponent(prompt);
      url = `https://replit.com/?prompt=${encoded}&referrer=replit-docs&${utm}`;
    } else {
      url = `https://replit.com/?prompt=${encodeURIComponent(prompt)}&referrer=replit-docs&${utm}`;
    }
    window.open(url, "_blank", "noopener,noreferrer");
  };
  ensureLZString();
  const baseButtonStyle = {
    display: "inline-flex",
    alignItems: "center",
    gap: "6px",
    padding: "8px 16px",
    borderRadius: "8px",
    fontSize: "14px",
    fontWeight: 500,
    cursor: "pointer",
    lineHeight: 1.2,
    fontFamily: "inherit",
    textDecoration: "none"
  };
  return <div style={{
    display: "flex",
    gap: "12px",
    margin: "16px 0",
    flexWrap: "wrap",
    alignItems: "center"
  }}>
      <button type="button" onClick={handleCopy} style={{
    ...baseButtonStyle,
    background: "transparent",
    color: "inherit",
    border: "1px solid rgba(127,127,127,0.4)"
  }}>
        Скопировать промпт
      </button>
      <button type="button" onClick={handleBuild} style={{
    ...baseButtonStyle,
    background: "#F26207",
    color: "#FFFFFF",
    border: "1px solid #F26207"
  }}>
        Создать в Replit ↗
      </button>
    </div>;
};

export const AiPrompt = ({children}) => {
  return <CodeBlock className="relative block max-w-full overflow-x-hidden font-sans !whitespace-pre-wrap !break-words" style={{
    overflowWrap: "anywhere",
    wordBreak: "break-word"
  }}>
      <div className="pr-7" style={{
    whiteSpace: "pre-wrap",
    overflowWrap: "anywhere",
    wordBreak: "break-word"
  }}>
        {children}
      </div>
    </CodeBlock>;
};

⏰ *Примерное время: 15 минут*

Прототип — это минимальная версия идеи продукта, которую уже можно кому-то показать, чтобы человек её опробовал и высказал своё мнение.

В качестве примера возьмём **Pace** — премиальный трекер бега, который даёт бегунам наглядный обзор тренировок, восстановления и прогресса.

<Frame>
  <img src="/assets/mirror/pace-running-tracker-7c5b75e07856.png" alt="Прототип Pace, опубликованный по публичному URL — дашборд премиального трекера бега с недельной дистанцией, недавними пробежками, предстоящими тренировками и достижениями" width="2880" height="1800" data-path="images/build-examples/pace-running-tracker.png" />
</Frame>

<PromptActions prompt={PACE_PROMPT} campaign="docs-prototype-an-idea" />

<div id="section">
  ## Что вы узнаете
</div>

Вы узнаете:

* Как превратить общую идею продукта в единый тестируемый сценарий.
* Как использовать режим Plan, чтобы определить рамки прототипа до начала разработки.
* Как оценить, достаточно ли план Agent компактен для тестирования.
* Как собирать обратную связь в контексте с помощью аннотаций и совместной работы в Canvas.
* Как параллельно изучать два варианта прототипа, когда направление развития продукта неясно.

<Steps>
  <Step title="Написание промптов">
    Приведённый выше промпт описывает **Pace** — личный трекер пробежек с аккуратной домашней панелью, потоком Add Run, статистикой, экраном плана-заглушки и реалистичными примерами данных по Сан-Франциско. Чтобы вставить его прямо в Replit, нажмите **Copy the prompt**, а чтобы открыть Replit с уже прикреплённым промптом — **Build on Replit**.

    Промпт намеренно конкретен в том, что касается визуального стиля, экранов, тестовых данных и того, что остаётся заглушкой, — именно это даёт Agent чёткие ориентиры для оценки.
  </Step>

  <Step title="Использование режима Plan (необязательно)">
    Чтобы Agent сначала продумывал направление продукта, прежде чем писать код, включите **Plan** рядом с полем ввода промпта. Режим Plan создаёт структурированный план, который можно просмотреть перед одобрением реализации — это полезно, когда направление ещё не определено окончательно.

    <Frame>
      <img src="/assets/mirror/agent-plan-mode-prompt-059fe8d2c277.png" alt="В конструкторе промптов Replit введён бриф прототипа Pace, а флажок Plan выделен — Agent готов определить объём работы до начала разработки" width="1440" height="900" data-path="images/fitstart/work-with-agent/agent-plan-mode-prompt.png" />
    </Frame>

    Проверьте план: целевых пользователей, основные сценарии, что будет реализовано, что будет имитировано и контрольный список тестирования. Если в плане слишком много функций, попросите Agent сократить его.

    <AiPrompt>
      Этот прототип слишком обширный.<br />
      Сократи его до одного целевого пользователя, одного основного сценария и одного вопроса, на который помогут ответить отзывы.<br />
      Перенеси остальные идеи в список будущих улучшений.
    </AiPrompt>
  </Step>

  <Step title="Проверка результатов работы Agent">
    Когда Agent завершит работу, откройте Preview и попробуйте поработать с Pace так, как это сделал бы человек, впервые зашедший в приложение.

    Проверьте следующее:

    * Отображается ли дашборд Alex Morgan, заполненный реалистичными примерами пробежек.
    * Можно ли записать новую пробежку с помощью плавающей кнопки **+** или кнопки **Start a Run** в главном блоке.
    * Появляется ли пробежка в разделе **Recent Runs** и обновляется ли статистика **This Week**.
    * Можно ли переключаться между вкладками Runs, Stats, Plan, Profile.
    * Можно ли сузить Preview до ширины экрана телефона, чтобы проверить мобильную вёрстку.

    Если что-то работает не так, опишите Agent, в чём проблема, и попросите внести минимальное исправление.

    <Frame>
      <img src="/assets/mirror/fitstart-preview-1b631e7ed180.png" alt="Прототип Pace, запущенный в Preview — слева в рабочем пространстве чат Agent, справа — заполненный дашборд Pace. Это то, что видит и с чем взаимодействует человек, впервые открывший приложение" width="1440" height="900" data-path="images/fitstart/fitstart-preview.png" />
    </Frame>
  </Step>

  <Step title="Опубликуйте и запросите обратную связь">
    Когда основные сценарии работают, публикуйте приложение.

    <Frame>
      <img src="/assets/mirror/fitstart-publish-dialog-137f98d0f7cd.png" alt="Project Editor с двумя точками входа в публикацию: встроенная карточка Publish с полем поддомена в Agent-чате и плитка Publishing в панели Tools and files" width="1440" height="900" data-path="images/fitstart/fitstart-publish-dialog.png" />
    </Frame>

    После публикации откройте публичный URL в новой вкладке браузера и убедитесь, что у человека, который заходит впервые, всё работает как надо, — и только потом делитесь ссылкой.

    <Frame>
      <img src="/assets/mirror/pace-running-tracker-7c5b75e07856.png" alt="Опубликованный прототип Pace, открытый в новой вкладке браузера — готов к тому, чтобы поделиться им с ревьюерами для получения предметной обратной связи" width="2880" height="1800" data-path="images/build-examples/pace-running-tracker.png" />
    </Frame>

    Отправляйте ссылку вместе с одной конкретной задачей:

    > Попробуйте Pace как бегун, который хочет отслеживать свои тренировки в течение недели. Запишите одну пробежку, посмотрите дашборд и расскажите, что показалось непонятным, а что — мотивирующим.

    Хорошая обратная связь должна помогать решить, что делать дальше. Не спрашивайте &quot;Что вы думаете?&quot;, если не хотите получить размытые ответы.
  </Step>

  <Step title="Совместная работа с командами">
    Прототипы продукта получаются лучше, когда участники команды могут смотреть на один и тот же артефакт и давать обратную связь прямо в контексте.

    Приглашайте соавторов в зависимости от того, насколько тесно вам нужно работать вместе:

    * Тех, кто будет смотреть только этот прототип или помогать с ним, приглашайте в проект. См. [Приглашение участников команды](/ru/ko/build/invite-teammates).
    * Тех, кто входит в постоянную команду и кому нужен доступ к нескольким проектам, приглашайте в рабочее пространство. См. [Командные рабочие пространства](/ru/ko/features/collaboration/team-workspaces).

    <Frame>
      <img src="/assets/mirror/agent-invite-dialog-2703a335500c.png" alt="Диалог приглашения, открытый на верхней панели рабочего пространства — поле ввода имени пользователя или электронной почты выделено, всё готово к приглашению участника команды в проект" width="700" height="400" data-path="images/fitstart/work-with-agent/agent-invite-dialog.png" />
    </Frame>

    А когда обратная связь носит визуальный характер, используйте [Canvas](/ru/ko/design/canvas). Добавляйте заметки прямо на прототип, отмечайте области, которые хотите изменить, и просите Agent учесть эти заметки при обновлении App.

    <AiPrompt>
      Use the Canvas annotations on Pace to improve the prototype.<br />
      Keep the run-logging flow unchanged.<br />
      Apply the notes to the hero section, weekly progress card, and recent runs list only.<br />
      After the update, test that logging a run still updates today&#39;s progress.
    </AiPrompt>

    <Frame>
      <img src="/assets/mirror/agent-canvas-annotations-c88ab5856966.png" alt="Frame Pace в Canvas — прямоугольник, стрелка и текстовая подпись, указывающие на расположение кнопки «Log in with Strava» рядом с кнопкой Log Run. Именно такие контекстные заметки соавторы могут оставлять для Agent" width="1440" height="900" data-path="images/fitstart/work-with-agent/agent-canvas-annotations.png" />
    </Frame>

    Для продакт-менеджеров это означает, что прототип работает как общий артефакт: дизайнеры могут комментировать пользовательские потоки, инженеры — оценивать реализуемость, а заинтересованные стороны — опробовать опубликованную версию.
  </Step>
</Steps>

<div id="section-2">
  ## 🎉 Поздравляем
</div>

Вы превратили идею продукта в сфокусированный прототип, которым можно поделиться, — работающий дашборд Pace, который коллеги и клиенты могут опробовать и оценить. Самым сложным было не создать этот прототип. Важно было научиться сводить широкую идею к одному тестируемому сценарию, собирать отзывы в контексте и решать, что стоит разрабатывать дальше.

<div id="section-3">
  ## Дальнейшие шаги
</div>

Опираясь на отзывы, выберите один из следующих шагов:

<CardGroup cols={2}>
  <Card title="Добавить базу данных" icon="database" href="/ru/ko/build/add-database">
    Сохраняйте записи о пробежках даже после обновления страницы.
  </Card>

  <Card title="Добавить вход в систему" icon="lock" href="/ru/ko/build/add-login">
    Дайте каждому бегуну учётную запись с собственным прогрессом.
  </Card>

  <Card title="Собрать мобильное приложение" icon="mobile-screen" href="/ru/ko/build/mobile-app">
    Возьмите Pace с собой на iOS или Android — отслеживание в первую очередь для телефона.
  </Card>

  <Card title="Создать дашборд на основе данных" icon="chart-line" href="/ru/ko/build/dashboard">
    Превратите записи о пробежках в наглядную картину прогресса.
  </Card>
</CardGroup>

<div id="section-4">
  ### Параллельная разработка
</div>

Когда вопрос о направлении развития продукта касается не только реализации, параллельная проработка нескольких идей позволяет сравнить варианты, а не выбирать один слишком рано. Replit Agent поддерживает параллельные задачи — вы можете поручить Agent одновременно работать над двумя и более прототипами, функциями или экспериментами и сравнить результаты бок о бок.

<Frame>
  <img src="/assets/mirror/task-plan-sidebar-ecab81b06bb0.png" alt="Представление треда с несколькими задачами Agent, выполняющимися параллельно, с индикаторами состояния" width="3430" height="1986" data-path="images/replitai/task-plan-sidebar.png" />
</Frame>

О том, как задачи попадают в очередь, выполняются и завершаются, читайте в разделе [Система задач](/ru/ko/core-concepts/agent/task-system).

Если вы не уверены, какое направление лучше, попросите Agent создать две параллельные задачи. В случае с Pace можно сравнить:

* **Прототип с упором на мотивацию**: серии дней, достижения, поддерживающие рекомендации.
* **Прототип с упором на аналитику**: более глубокая статистика, тренды по неделям, личные рекорды.

<AiPrompt>
  Create two parallel tasks for improving Pace as a product prototype.<br />
  Task 1 should explore a motivation-first version with streaks, achievements, and encouraging recommendations.<br />
  Task 2 should explore an analytics-first version with deeper stats, week-over-week trends, and personal records.<br />
  Keep both versions small enough to test today.<br />
  For each task, define the user flow, what to build, and how I should evaluate the result.
</AiPrompt>

Когда обе задачи будут выполнены, сравните их по одному и тому же вопросу:

> Какая версия с большей вероятностью заставит вернуться завтра и почему?

<div id="section-5">
  ## Нужна дополнительная помощь?
</div>

* **Если в прототипе слишком много функций:** попросите Agent сократить его до одного целевого пользователя и одного сценария.
* **Если аудитория неясна:** попросите Agent переписать приветственный раздел под одного целевого пользователя.
* **Если тестовые данные скрывают риски:** попросите Agent составить список того, что работает по-настоящему, а что является заглушкой.
* **Если отзывы расплывчаты:** дайте рецензенту одну задачу и один вопрос.
* **Если публикация не удалась:** воспользуйтесь разделом [Устранение неполадок с публикацией](/ru/ko/build/troubleshooting).

<div id="section-6">
  ## Связанные материалы
</div>

* [Создание и публикация первого приложения](/ru/ko/build/your-first-app)
* [Вайб-кодинг 101](/ru/ko/learn/foundations/vibe-coding-101)
* [Приглашение участников команды](/ru/ko/build/invite-teammates)