---
title: "Microsoft FabricでデータアプリをビルドするMicrosoft Fabric"
description: "Replit AgentでMicrosoft Fabricのデータアプリをビルドして公開します。"
sidebarTitle: "Microsoft Fabric"
icon: "chart-column"
---

<Frame>
  <img src="/assets/mirror/fabric-template-selector-d3f975e2e1d0.png" alt="Templatesドロップダウンが開いているReplitホームページ、Org templatesの下にMicrosoft Fabric Appがハイライト表示" width="872" height="406" data-path="images/connectors/microsoft-fabric/fabric-template-selector.png" />
</Frame>

ReplitでAI搭載アプリをビルドし、エンタープライズグレードのガバナンスとセキュリティを組み込んだ状態でMicrosoft Fabricに直接公開します。Microsoft Fabricインテグレーションは、ガバナンスされたエンタープライズデータをダッシュボード、ワークフロー、AIアプリケーションなどの内部ツールに変換します。チームはプロンプトから本番環境へと迅速に進み、認証とアクセスポリシーはすでに信頼しているFabric環境の中に留まります。

## Fabricデータアプリとは

FabricデータアプリはPower BIセマンティックモデルに対して[DAX（Data Analysis Expressions）](https://learn.microsoft.com/en-us/dax/)クエリを実行してPower BIチャートをレンダリングする静的なReactアプリです。構築する際に考慮すべき特徴があります：

* **Power BIセマンティックモデルがデータソース。** チャートは接続するセマンティックモデル（データセット）に対するDAXクエリによって駆動されます。
* **設計上は読み取り専用。** アプリはDAXの `EVALUATE` でデータを読み取ります。Power BIに書き戻しはしません。書き込み可能な状態が必要な場合は、ダッシュボードと並行して別のアプリケーションデータベースを使用してください。
* **Fabricポータルを通じてレンダリング。** データレイヤーはMicrosoft Fabricポータル内で実行され、セマンティックモデルに対してクエリを実行してアプリに結果を返します。データはFabric環境内に留まります。
* **Microsoftインフラでホスト。** 完成したデータアプリをMicrosoftのホスティングに公開して、Fabric環境の残りと並行して実行できます。

<Note>
  Microsoft FabricデータアプリにはPower BIセマンティックモデルを持つMicrosoft Fabricテナントへのアクセスが必要で、プレビュー期間中は承認されたテスタープログラムへの参加が必要です。
</Note>

バックグラウンドでは [Rayfin](https://github.com/microsoft/rayfin)（アプリケーションバックエンドをビルドするためのオープンソースSDKとCLI）がAgentにFabricデータの上に本番対応アプリを定義・公開させます。

単一のワークフローで作業できます：アプリについて説明し、Agentにスキャフォールドさせ、ガバナンスされたFabricデータに接続し、組織のFabricテナント内で実行するよう公開します。

このガイドではビルダーフロー、アプリがFabricデータに接続する方法、チームと自動化のオプションを説明します。他のウェアハウスを基盤とした一般的なデータダッシュボードについては [データビジュアライゼーション](/ja/features/artifact-types/data-apps) を参照してください。

## ユースケース

Microsoftデータエコシステムにすでに投資している組織にとって、これは内部ソフトウェアへのはるかに速いパスです。認証、API、デプロイメントツール、ガバナンスポリシーの配線に数週間費やす代わりに、チームは何が欲しいかを説明してAgentにビルドさせます。

一般的なユースケースには次が含まれます：

* 部門特化の生産性ツール
* オペレーションダッシュボード
* データ探索ツール
* ワークフロー自動化アプリ
* レポートインターフェース

エンタープライズITにとって、ガバナンス、認証、セキュリティはすでに管理している既存のFabric環境内に留まるため、迅速なアプリ作成が集中管理のコストをもたらしません。

## Fabricデータアプリをビルドする

ビルダーフローは会話型です。認証とデータアクセスはプロンプトに従ってMicrosoftを通じて行われる間、ずっとReplitの中にいます。

<Steps>
  <Step title="Fabricテンプレートから始める">
    Replitホームページで **Templates** ドロップダウンを開き、**Org templates** の下から **Microsoft Fabric App** を選択します。これでAgentがFabricツールをプリロードしてデータアプリワークフローに入ります。

    <Frame caption="ReplitホームページでMicrosoft Fabric Appのorgテンプレートを選択（プライベートプレビューUI）。">
      <img src="/assets/mirror/fabric-template-selector-d3f975e2e1d0.png" alt="Templatesドロップダウンが開いているReplitホームページ、Org templatesの下にMicrosoft Fabric Appがハイライト表示" width="872" height="406" data-path="images/connectors/microsoft-fabric/fabric-template-selector.png" />
    </Frame>
  </Step>

  <Step title="欲しいダッシュボードについて説明する">
    ビルドするダッシュボードまたはレポートツールを説明するプロンプトを入力します。例えば：「FabricのセールスモデルからMoMの売上成長ダッシュボードをビルドして」。DAXを書く必要はありません — Agentがクエリを生成します。
  </Step>

  <Step title="Microsoftでサインインする">
    プロンプトが表示されたら、FabricデータのあるテナントのMicrosoft OAuthでサインインします。これによりAgentがアクセス可能なセマンティックモデルを読む権限が与えられます。サインインするアイデンティティは使用するFabricワークスペースへのアクセス権を持っている必要があります。
  </Step>

  <Step title="データソースを追加する">
    Agentに求められたら、使用したいPower BI / FabricセマンティックモデルをシェアしてくださいFabricポータルからのフルデータセットURL（例：`https://app.powerbi.com/groups/<workspaceId>/datasets/<datasetId>/...`）またはワークスペースIDとデータセット/アイテムIDを提供できます。Agentはこれらをクエリするワークスペースとセマンティックモデルを識別するために使用します。

    モデルはPower BIサービスに**公開済み**である必要があります — ローカルの `.pbix` ファイルは使用できません。

    <Frame caption="AgentがPower BI / Fabricセマンティックモデルリンクを要求（プライベートプレビューUI）。">
      <img src="/assets/mirror/fabric-agent-semantic-model-prompt-732ce9c4a29d.png" alt="ビルダーにPower BIまたはFabricセマンティックモデルをFabricポータルのフルURLまたはワークスペースIDとデータセット/アイテムIDとして共有するよう求めるAgentパネル、ローカルの.pbixファイルはモデルがPower BIサービスに公開されている必要があるため使用できないことを注記付き" width="1316" height="650" data-path="images/connectors/microsoft-fabric/fabric-agent-semantic-model-prompt.png" />
    </Frame>
  </Step>

  <Step title="Agentにビルドと反復をさせる">
    Agentはセマンティックモデルのスキーマを探索し、DAXクエリを書き、適切なチャートでダッシュボードを組み立てます。結果をレビューして自然言語で変更を依頼してください — フィルタを追加する、チャートタイプを変更する、別のメトリクスを追加するなど。
  </Step>

  <Step title="公開する（オプション）">
    ダッシュボードの準備ができたら、Microsoftのホスティングに公開してFabric環境内で実行できます。公開ストーリーは別途説明されます。このガイドはデータへの接続とアプリのビルドに焦点を当てています。
  </Step>
</Steps>

## Fabricデータへの接続

データを読み取るには、アプリにMicrosoft Fabricへの認証済み接続が必要です。Replitはその接続を2つの方法のいずれかで確立します。

### アプリレベルのFabricサインイン（閲覧者向け）

公開されたデータアプリの閲覧者はコネクタを設定する必要はありません。アプリに埋め込まれたMicrosoft Fabricシングルサインオンで認証し、アプリはFabricポータルを通じて閲覧者のアイデンティティでデータを読み取ります。これにより、アクセスは既存のFabricパーミッションによってガバナンスされ続けます。

### Microsoft Fabricコネクタ（ビルダーへの簡単なパス）

ビルダーにとって、**Microsoft Fabricコネクタ**は開発中のデータアクセスを認可する便利な方法です。オプションです — 簡単なパスであり、要件ではありません。

* 最初にセットアップするビルダーがMicrosoftとの**一度きりのOAuth**を完了します。
* **同じプロジェクトの他のコラボレータ**はその接続を継承します。
* **異なるワークスペース**はコネクタを有効化して独自のOAuthを完了する必要があります。
* OAuth済みのアイデンティティは対象のFabricワークスペースへのアクセス権を持っている必要があります。

コネクタはMicrosoft Entra OAuthを使用し、テナントにスコープされています。他のReplitコネクタと同様に、プロジェクトの [インテグレーション](/ja/features/integrations/overview) ページから管理します。コネクタの概念全般については [コネクタの管理](/ja/replitai/managing-connectors) を参照してください。

<Note>
  プライベートプレビュー期間中は、Microsoft FabricコネクタのReplit管理の共有クレデンシャルはありません。ビルダーは自身のMicrosoft Entraテナントに対して認可します。
</Note>

## チームと自動化

チームでの共有使用や非インタラクティブな環境（CIなど）では、インタラクティブなOAuthが常に実用的とは限りません。そのような場合は、インタラクティブにサインインする代わりに、プロジェクトの [シークレット](/ja/core-concepts/project-editor/app-setup/secrets) を通じて**サービスプリンシパル**トークンを提供します。アプリはサービスプリンシパルのアイデンティティを使用してFabricデータを読み取ります。

これはマシン間セットアップの最も近い類似物です：Microsoft Entraでプロビジョニングされ、Fabricワークスペースアクセス権が付与された非人間アイデンティティが、人の介在なしにアプリを認証します。サービスプリンシパルがアプリがクエリするワークスペースとセマンティックモデルへのアクセス権を持っていることを確認してください。

## トラブルシューティング

<AccordionGroup>
  <Accordion title="アプリプレビューでデータが読み込まれない">
    FabricデータレイヤーはマイクロソフトのFabricポータルを通じて実行されます。チャートが空でレンダリングされるか、アプリプレビューがデータの読み込みに失敗する場合は、Microsoftサインインを完了したこと、およびサインイン済みのアイデンティティがリンクしたワークスペースとセマンティックモデルへのアクセス権を持っていることを確認してください。
  </Accordion>

  <Accordion title="Agentがセマンティックモデルを見つけられない">
    FabricポータルからPower BIデータセットリンクを貼り付け、アイデンティティ（またはサービスプリンシパル）がそのワークスペースへの少なくとも読み取りアクセス権を持っていることを確認してください。AgentがワークスペースやモデルのResolveできないと報告した場合はデータセットリンクを再共有してください。
  </Accordion>

  <Accordion title="コネクタがスタックしているか繰り返し接続プロンプトが表示される">
    プロジェクトの [インテグレーション](/ja/features/integrations/overview) ページからMicrosoft Fabricコネクタを再認可してください。OAuth済みのアイデンティティがFabricワークスペースアクセス権を持っていることを確認してください。チームやCIでの使用には、シークレットを通じて提供するサービスプリンシパルトークンを優先してください。
  </Accordion>
</AccordionGroup>

## 関連ドキュメント

* [データビジュアライゼーション](/ja/features/artifact-types/data-apps)：Agentでダッシュボードとレポートツールをビルドします。
* [ウェアハウスコネクタの概要](/ja/connectors/warehouses/overview)：サポートされているすべてのウェアハウスコネクタ。
* [コネクタの管理](/ja/replitai/managing-connectors)：コネクタの管理と設定。
* [Microsoft Fabricドキュメント](https://learn.microsoft.com/en-us/fabric/)：FabricとPower BIのMicrosoftドキュメント。
